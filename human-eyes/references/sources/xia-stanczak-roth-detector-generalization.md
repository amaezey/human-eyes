# Explaining Generalization of AI-Generated Text Detectors Through Linguistic Analysis

## Metadata

- **URL:** https://aclanthology.org/2026.eacl-long.307/
- **Author / owner:** Yuxi Xia, Kinga Stańczak, and Benjamin Roth
- **Published:** March 2026
- **Retrieved:** 2026-07-14
- **Extracted:** 2026-07-14
- **Source type:** peer-reviewed empirical conference paper
- **Evidence tier:** Peer-reviewed / academic empirical
- **Review mode:** new
- **Stable identifier:** DOI 10.18653/v1/2026.eacl-long.307; ACL Anthology ID 2026.eacl-long.307
- **Version / revision:** EACL 2026 final proceedings version, pages 6524-6546
- **Full-text status:** complete
- **Snapshot:** `snapshots/xia-stanczak-roth-detector-generalization.md`
- **Extraction method:** canonical ACL PDF downloaded and converted from its embedded text layer with `pdftotext -layout`; 23-page structure and beginning, middle, and end checked against the rendered PDF; canonical PDF preserved under `snapshots/attachments/`
- **Snapshot SHA-256:** `d0fe66bed9ddd8d81ad34fb2c5568220b478fa687270d83389f5326a383261cf`
- **Model / corpus scope:** English binary human-versus-AI classification; 516,000 texts comprising 12,000 human texts and 504,000 generated counterparts; arXiv abstracts, CNN/Daily Mail news, Amazon Reviews 2023 reviews, and ASQA answers; Mistral-Large-Instruct-2411, DeepSeek-R1-Distill-Llama-70B, Llama-3.3-70B-Instruct, Qwen2.5-72B/32B/14B-Instruct, and solar-pro-preview-instruct; six prompt conditions; XLM-RoBERTa-base and DeBERTa-V3-small detectors
- **Access limitations:** none for the full text; wide tables and multi-column order are mechanically interleaved in the text extraction, so the preserved PDF remains authoritative for visual layout

## Summary

This EACL 2026 paper studies why fine-tuned AI-text classifiers lose accuracy across unseen prompts, generator models, and domains. The authors build a controlled 516,000-text benchmark from four English domains, seven generator configurations, and six prompt strategies, then train XLM-RoBERTa-base and DeBERTa-V3-small classifiers for each condition. They correlate cross-condition accuracy with shifts in 80 surface linguistic features. The strongest project contribution is not a new universal writing tell: it is direct evidence that in-domain detector success can hide prompt-, model-, domain-, and architecture-specific reliance on different linguistic cues. The paper therefore strengthens human-eyes' existing non-authorship boundary and its need for register-, model-, prompt-, and corpus-aware evaluation.

## Main insights

- In-domain classifier accuracy is usually near perfect, but transfer is uneven: the 3-shot prompt condition is the hardest cross-prompt case, with reported accuracy falling to 80-89%, and abstract-trained detectors fall as low as 57% when transferred across domains.
- The benchmark separates prompt, model, and domain shifts instead of collapsing them into one out-of-distribution score. It includes 0-shot, 3-shot, style imitation, 0-shot chain-of-thought, 1-shot chain-of-thought, and iterative self-refinement.
- Linguistic feature shifts correlate with detector transfer in some settings, but the useful feature depends on the classifier, prompt, generator, and domain. The paper explicitly reports no universal linguistic signal.
- Examples include a 0.416 correlation between DeBERTa cross-model accuracy and shifts in the human-AI past-tense gap, a 0.385 correlation between RoBERTa cross-model accuracy and shifts in the human-AI “It” pronoun gap, and correlations above 0.7 between cross-prompt accuracy and short-sentence-gap shifts in some dataset-model configurations. These are not correlations with raw feature frequencies in individual documents.
- After Bonferroni and false-discovery-rate correction, many cross-model relationships remain significant, but no individual feature remains significant for cross-dataset transfer. The authors interpret cross-domain failures as broader distribution shift rather than one dominant cue.
- Different detector architectures trained on the same material rely on different features. A writing feature cannot be treated as a stable detector explanation without naming the detector and evaluation condition.
- Generated text was cleaned to remove formulaic assistant openings, headings, lists, placeholders, metadata, non-linguistic symbols, and model-specific reasoning tags. The reported transfer gaps therefore persist after obvious workflow and formatting residue is removed.
- Cross-model transfer is asymmetric in a concrete case: detectors trained on Qwen or Solar outputs perform poorly on Llama-generated text, while transfer among the other generators is more stable.
- The paper describes cross-dataset transfer as “moderately” correlated with passive voice, but its aggregate coefficients are 0.296 for DeBERTa and 0.287 for RoBERTa. Under the paper's own bands, these values fall in the low band, so the qualitative label and reported values are inconsistent and should both be preserved.
- The paper is English-only, uses two fine-tuned encoder classifiers, analyzes correlations rather than causal interventions, and covers a finite set of domains, models, and prompts. Its specific correlations are not thresholds or authorship rules for individual documents.
- The 80 surface features omit other plausible drivers including semantic coherence and discourse structure; the authors present the analysis as a linguistic perspective, not a comprehensive account of generalization.
- Several generic prose claims in the feature-definition sections, such as AI readability, positivity, passive-voice, and lexical-density tendencies, are inherited from cited studies rather than established by this paper as universal effects.

## Evidence and claims to extract

- **Direct source reviewed:** EACL 2026 final proceedings PDF, DOI 10.18653/v1/2026.eacl-long.307, all 23 pages including appendices, prompt templates, full feature table, case studies, captions, limitations, and references.
- **Method and sample:** 3,000 human texts per domain split 50:17:33 and 3,000 generated counterparts for every model-prompt-domain combination; 12,000 human plus 504,000 generated texts; four English domains; seven model configurations from five named families; six prompt strategies; separate binary DeBERTa and XLM-RoBERTa detectors trained for condition-specific cross-prompt, cross-model, and cross-dataset tests; 80 linguistic feature metrics; absolute Pearson correlations with Bonferroni, Benjamini-Hochberg FDR, and Spearman robustness checks. Human-source selection uses abstracts and news of at least 1,000 characters, reviews of at least 350 characters, and the longest available QA texts. Generated texts are topic-matched and length-targeted through character-count information in the prompts rather than guaranteed to be length-matched. Detector input is truncated to the 512-token maximum supported by XLM-RoBERTa.
- **Direct versus cited evidence:** C01-C08 and C10-C12 below are direct design, result, robustness, cleaning, or limitation claims from this paper. C09 distinguishes the paper's own results from background statements about readability, sentiment, passive voice, lexical density, and prior detector performance that the paper attributes to earlier sources.
- **Important limits and counterexamples:** Near-perfect in-domain results coexist with large transfer losses; Qwen- or Solar-trained detectors can transfer poorly to Llama outputs; strong correlations appear only in some configurations; no one feature explains all settings; cross-dataset feature correlations do not survive strict multiple-testing correction; the paper calls the 0.296/0.287 passive-voice cross-dataset correlations moderate even though its own bands classify these values as low; correlation is not causation; English-only encoder results may not transfer to other languages or detector architectures; 512-token detector truncation limits the analyzed input; generated counterparts are prompted from human-source metadata and length targets, so the benchmark is controlled rather than a naturalistic prevalence sample; and the feature inventory does not cover semantic coherence or discourse structure.

## Skill-use audit

- **Good use:** Support evaluation designs that separate prompt, generator, detector architecture, and domain; justify reporting out-of-distribution failures and null results; motivate model/version/prompt metadata; treat linguistic features as conditional, distributional evidence; reinforce the product's non-authorship boundary.
- **Misuse / overclaim:** Using past tense, passive voice, pronouns, short sentences, or any reported correlation as a universal AI tell, an individual-document rule, or a causal explanation of detector behavior.
- **Unsupported use:** Claiming the results generalize to multilingual prose, generative or retrieval-based detectors, current models outside the benchmark, mixed human-AI documents, naturally occurring undisclosed use, or human-eyes' current corpus without new evaluation.
- **Underused evidence:** The project does not currently run factorial cross-prompt, cross-model, cross-domain comparisons or multiple-hypothesis-corrected feature-shift analysis. It also does not treat obvious-residue-cleaned material as a distinct evaluation condition.
- **Patterns left on the table:** Readability, sentiment, POS, and lexical-density metrics remain candidate research dimensions. Voice, pronoun frequency, and short-sentence rate were measured on the project corpora and became #68, #69, and a branch of #25 in 2026-07-26 (DR-66); tense was measured and produced no check.

## Matched patterns / rules

- `sentence-length-variance` / pattern #52: partial conceptual overlap with short-sentence and sentence-length distribution features, but the live check uses one fixed within-document standard-deviation threshold rather than condition-specific distribution shifts.
- `vocabulary-diversity` / pattern #53: partial overlap with lexical diversity, but the live check uses coarse type-token ratio and is not one of the paper's stable universal explanations.
- `paragraph-length-uniformity` and `overall-signal-stacking`: relevant as current distributional and aggregate checks, but the paper does not validate their thresholds.
- `referential_clarity` in `judgement.json`: mentions pronouns for antecedent clarity; it does not measure pronoun frequency. Pronoun frequency is measured separately by programmatic #69 `no-it-pronoun-rate` since 2026-07-26 (DR-66).
- `even_jargon_distribution` in `judgement.json`: a document-level distribution judgement, not a substitute for the paper's cross-condition feature-shift analysis.
- `no-collaborative-artifacts`, `no-placeholder-residue`, `no-excessive-lists`, `no-unicode-flair`, and `no-em-dashes`: the paper removed comparable artifacts before evaluation, demonstrating that classifier transfer gaps remain after those surface cues are absent. Coverage is not exact: `no-section-scaffolding` catches repeated identical labels rather than arbitrary section titles, and no deterministic check targets model reasoning tags such as `\think` plus preceding reasoning.

## Associated hypotheses

- H1, continuous calibrated register-distance score per pattern.
- H3, drop detection framing entirely.
- H12, genre-aware threshold calibration.
- H22, long-tail compression and grammatical standardisation.
- H24, register-specific vocabulary density.
- H25, model-family versus generic-AI residue.
- Proposed follow-up hypothesis: detector explanations are condition-specific; feature stability should be tested across prompt, generator, domain, and detector architecture before a metric affects product behavior.

## Questions / follow-up

- Should a future evaluation round add a small factorial prompt-model-domain matrix, or only record these dimensions as provenance until the corpus is large enough?
- Should obvious-residue-cleaned AI samples become a separate adversarial/robustness lane rather than entering the main literal-first comparison corpus?
- Direct review of the background sources would be required before any cited readability, sentiment, passive-voice, or lexical-density claim could support a product recommendation.

## Update provenance

Not applicable: initial ingestion.

## Decision history

- 2026-07-26 (DR-66): all four named linguistic features were measured on the project corpora, because the paper reports correlations between detector accuracy and feature-gap shifts rather than any human-versus-AI direction. Passive voice became programmatic #68, the `it` pronoun programmatic #69, and the short-sentence rate a new branch of #25. Past tense got no check. C04, C09, and C11 are ruled.
- 2026-07-26 (DR-66 orphan sweep): C05, C06, C08, and C10 carried no register row and were queued as DR-160, DR-161, DR-162, and DR-163. C01, C02, and C03 sit under pending DR-65 and DR-83. C07 and C12 are record-only limitations with nothing to rule.

## Project coverage

This is the authoritative review table. Give every relevant source claim or example a stable claim ID in the first cell.

Escape any literal pipe inside any table cell as `\|`; an unescaped extra pipe makes the row invalid.

| Source claim or example | Evidence assessment | Existing project coverage | Missing or challenged | Recommendation | User decision | Implementation status |
|---|---|---|---|---|---|---|
| C01: The study isolates cross-prompt, cross-model, and cross-dataset transfer using 516,000 controlled English texts, seven generator configurations, six prompts, four domains, and two detector architectures. Human sources are abstracts/news of at least 1,000 characters, reviews of at least 350 characters, and the longest available QA texts; detector input is truncated to 512 tokens. | Direct peer-reviewed benchmark design; large controlled sample. Generated counterparts are topic-matched and length-targeted by putting character counts in prompts, not demonstrated to be exactly length-matched. The benchmark is not a naturalistic prevalence sample, and the 512-token classifier limit means longer source text is not fully analyzed. | Partly covered: `dev/TESTING.md` requires matched human and AI samples, broader genres/registers, prompt provenance, and explicit version refs; `dev/evals/corpus.json` provides multiple genres but no factorial prompt-model-domain design or detector-input truncation field. | No current evaluation decomposes prompt, generator, domain, and detector architecture as independent transfer axes. Source-selection thresholds and model input truncation can also change what distribution is actually evaluated. Self-refined/style-imitation samples are intentionally excluded from the main new-corpus comparison. | If approved, add a separate robustness-lane requirement to `dev/TESTING.md`, with prompt, generator, domain, evaluator version, source-selection rule, length target, and truncation fields; preserve prompts/outputs and report each axis separately. Do not add coached/self-refined samples to `dev/evals/corpus.json`. | pending | not started |
| C02: Near-perfect in-domain accuracy masks transfer failures; 3-shot cross-prompt accuracy falls to 80-89%, and abstract-trained cross-domain accuracy reaches 57%. | Direct classifier results; exact ranges are benchmark-specific; accuracy is not a human-eyes metric and cannot be imported as a product threshold. | Partly covered: `STRATEGY.md` rejects authorship classification; `dev/TESTING.md` distinguishes calibration from held-out claims and requires reversed/weak pairs; `dev/skill-workspace/latest-performance-report.md` shows the current N=5 paired corpus has modest and sometimes inverted gaps. H3 and H12 in `dev/hypotheses.md` already question generic detection framing and universal thresholds. | Current reports do not demonstrate performance under controlled distribution shift, and the narrow current corpus cannot quantify cross-condition reliability. | If approved, add an out-of-distribution reporting requirement to `dev/TESTING.md`: unseen-condition results, weak/reversed cases, uncertainty, and an explicit prohibition on converting these detector accuracies into a human-eyes authorship score. | pending | not started |
| C03: No universal linguistic signal explains generalization; useful cues vary by prompt, generator, domain, and detector architecture. | Direct correlation analysis across 80 features, with configuration-specific effects and explicit author qualification that features provide only a partial explanation. | Partly covered: H1, H12, H24, and H25 in `dev/hypotheses.md` anticipate calibrated, register-specific, and model-specific evidence. In `human-eyes/scripts/grade.py`, `sentence-length-variance`, `paragraph-length-uniformity`, and `vocabulary-diversity` live in `STATISTICAL_CHECKS`, while `overall-signal-stacking` lives in `AGGREGATE_CHECKS`. Pronoun clarity and document-level distribution readings such as `referential_clarity` and `even_jargon_distribution` are agent records in `human-eyes/scripts/judgement.json`. | The live statistical checks use fixed document-level thresholds, and the agent records use semantic readings; neither supplies the paper's cross-condition stability analysis. | If approved, add to `dev/TESTING.md` a stability gate before any new distributional metric can affect product behavior: test the same feature across multiple prompts, generators, domains, and evaluator architectures. Do not add a universal cue check to `human-eyes/scripts/grade.py` or `human-eyes/scripts/judgement.json` from this result alone. | pending | not started |
| C04: Detector accuracy correlates with shifts in the human-AI feature gap: 0.416 for DeBERTa cross-model accuracy versus past-tense-gap shifts, 0.385 for RoBERTa cross-model accuracy versus “It”-pronoun-gap shifts, and above 0.7 for cross-prompt accuracy versus short-sentence-gap shifts in some model-domain settings. | Direct reported correlations between generalization accuracy and train-test changes in human-versus-AI feature differences. They are not correlations with raw document-level feature frequencies. The absolute correlations are architecture- and condition-specific and do not establish direction, causality, or an individual-document rule. | Partly covered: `sentence-length-variance` in `human-eyes/scripts/grade.py` measures within-document sentence-length dispersion; `referential_clarity` in `human-eyes/scripts/judgement.json` reviews antecedents rather than pronoun frequency. No live check measures tense frequency or the paper's train-test feature-gap shifts. | Treating `referential_clarity` as pronoun-frequency coverage would be a category error. A fixed sentence-SD threshold does not reproduce cross-condition feature-gap shifts. | If approved, add a research-candidate note under H22/H25 in `dev/hypotheses.md` for tense, pronoun-frequency, voice, and short-sentence feature-gap stability. Do not add checks to `human-eyes/scripts/grade.py`; require register-matched human controls, explicit length/truncation controls, multiple-testing correction, and non-authorship wording before reconsideration. | approved | implemented |
| C05: Multiple-testing correction retains many cross-model feature relationships but no individual cross-dataset feature, suggesting broad domain shift rather than one dominant cue. | Direct Bonferroni, FDR, and Spearman robustness study; null result is central and limits feature-level interpretation. | Not covered: `dev/TESTING.md` asks for variation, weak pairs, and false positives, but current feature exploration does not specify multiple-hypothesis correction or a null-result ledger. | Exploratory feature expansion could promote chance correlations, especially when many surface metrics are tested on small corpora. | If approved, add to `dev/TESTING.md` a multi-feature-experiment requirement for a predeclared feature family, family-wise or false-discovery control (or justified resampling equivalent), effect sizes with uncertainty, and retained null results. | pending | not started |
| C06: Transfer gaps remain after generated text is cleaned of formulaic assistant openings, section titles, bullet/numbered lists, placeholders, metadata, asterisks/hash marks/triple dashes, and model-specific reasoning tags such as `\think` plus preceding DeepSeek reasoning. | Direct preprocessing design; it reduces trivial artifact leakage but does not prove every remaining cue is semantic or causal. | Partly covered and qualifying: `human-eyes/scripts/grade.py` includes `no-collaborative-artifacts`, `no-placeholder-residue`, `no-excessive-lists`, `no-unicode-flair`, and `no-em-dashes`. `no-section-scaffolding` covers repeated identical section labels only, not arbitrary section-title removal. No deterministic check exactly covers model reasoning tags such as `\think` or the preceding reasoning block. `STRATEGY.md` defines the covered items as writing problems rather than authorship proof. | The live checks remain useful editing signals but are not complete mirrors of preprocessing and cannot be sufficient detector evidence; a benchmark can remove the covered artifacts and still expose deeper classifier shifts. | Preserve existing checks with no product-logic change. If approved, add a raw-versus-residue-cleaned robustness lane to `dev/TESTING.md`, with same-source paired preprocessing and separate candidate-versus-threshold reporting. Do not add a `\think` detector from this paper alone. | pending | not started |
| C07: The findings are English-only, encoder-classifier-specific, correlational, and limited to the sampled models, prompts, and domains. | Direct limitations; the authors call for multilingual, architectural, causal, and newer-generation follow-up. | Fully covered in principle: `STRATEGY.md` disclaims authorship detection; `dev/TESTING.md` requires provenance, genre/register breadth, explicit refs, and cautious interpretation; source-card metadata records model and corpus scope. | The limitations were absent from the evidence library before this source record. | Record only in this source card; no product action and no edit to `STRATEGY.md`, `dev/TESTING.md`, or checker files. Independent source-record review remains required. | pending | not started |
| C08: Different detector architectures trained on the same data rely on different linguistic features. | Direct comparison between DeBERTa- and XLM-RoBERTa-based classifiers; supported by differing correlations, not mechanistic causal proof. | Partly covered: H25 in `dev/hypotheses.md` distinguishes model-family from generic AI residue, while current performance reports separate deterministic and agent-assessed findings; the pilot agent review documents evaluator-specific overcalls and misses. | H25 addresses generators more directly than detector/evaluator architecture. The project lacks a named hypothesis for evaluator-dependent explanations. | If approved, extend H25 or add a linked hypothesis in `dev/hypotheses.md` to cover detector/evaluator architecture; do not change `human-eyes/scripts/grade.py` or `human-eyes/scripts/judgement.json`. Verification requires the same samples evaluated by multiple evaluator variants with disagreements preserved. | pending | not started |
| C09: Generic claims about readability, positivity, passive voice, lexical density, and prior detector performance in the feature-definition sections are cited background, not universal direct findings of this paper. | Indirect evidence inherited from cited studies; this paper measures feature shifts and transfer correlations, not a universal AI-versus-human direction for every feature. | Fully covered by the source-ingest contract, `human-eyes/references/sources/README.md`, and `human-eyes/references/pattern-opportunities.md`, which require direct review before promotion and distinguish indirect evidence. | Those cited works do not all have claim-specific direct reviews tied to this card. | Record only: do not promote inherited feature-direction claims from this card. If a future recommendation depends on one, ingest the exact upstream source into `human-eyes/references/sources/` and review it independently first. | approved | implemented |
| C10: Detectors trained on Qwen or Solar outputs perform poorly on Llama-generated text, while transfer across the other generators is more stable. | Direct cross-model result in Section 6.2.2. The authors associate it with differences in past-tense and “It”-pronoun feature profiles, but that explanation is correlational rather than causal. | Partly covered: H25 in `dev/hypotheses.md` anticipates model-family-specific residue; current human-eyes evaluation does not run generator-to-generator transfer matrices. | The existing hypothesis does not preserve this asymmetric train-generator/test-generator failure case or require direction-specific transfer reporting. | If approved, add an asymmetric generator-transfer test requirement to H25 in `dev/hypotheses.md` and the robustness lane in `dev/TESTING.md`; report every train/test direction rather than an averaged model-family score. No checker change. | pending | not started |
| C11: Cross-dataset accuracy correlates with shifts in the human-AI passive-voice gap at 0.296 for DeBERTa and 0.287 for RoBERTa. The prose calls this “moderate,” although the paper's bands classify these values as low. | Direct Table 1 values and Section 6.2.3 wording, with an internal qualitative-label inconsistency. Neither value survives the paper's strict multiple-hypothesis correction as an individual cross-dataset feature. | Covered since 2026-07-26 by programmatic #68 `no-passive-voice-rate` in `human-eyes/scripts/grade.py`, which measures passive-verb frequency against the project corpora. That check does not rest on this correlation. | Repeating only “moderate” would overstate the paper under its own bands; repeating only the numeric values would omit the authors' interpretation and correction null. | Record only in this source card with values, wording inconsistency, and corrected-null context. No product action and no edit to `human-eyes/scripts/grade.py`. | approved | implemented |
| C12: The 80 surface linguistic features provide only a partial explanation; semantic coherence, discourse structure, and detector-specific inductive biases may also drive generalization. | Direct Section 6.4 qualification. The paper does not measure or isolate those omitted drivers. | Partly covered conceptually by semantic agent records in `human-eyes/scripts/judgement.json`, but those records are writing-quality assessments rather than explanations of detector transfer. | The benchmark cannot establish that its measured surface features comprehensively explain generalization, and current agent judgements cannot be substituted for the missing analysis. | Record only as a limitation. Do not treat `human-eyes/scripts/judgement.json` as detector-explanation coverage and do not add a new product check. | pending | not started |

## Recommendations

- C01: If approved, add the separate robustness-lane design to `dev/TESTING.md`, including source-selection, length-targeting, truncation, prompt, generator, domain, and evaluator fields; do not change `dev/evals/corpus.json` to add coached/self-refined samples.
- C02: If approved, add out-of-distribution reporting and the no-authorship-score restriction to `dev/TESTING.md`; no checker change.
- C03: If approved, add a cross-condition stability gate to `dev/TESTING.md`; do not convert the finding into a universal check in `human-eyes/scripts/grade.py` or `human-eyes/scripts/judgement.json`.
- C04: Ruled 2026-07-26 (DR-66). Measured on the project corpora rather than filed as research candidates: passive voice is programmatic #68, the `it` pronoun is programmatic #69, the short-sentence rate is a branch of #25, and past tense got no check.
- C05: If approved, add multiple-hypothesis control, effect-size uncertainty, and explicit null-result retention to `dev/TESTING.md`.
- C06: Preserve the existing residue checks unchanged; if approved, add raw-versus-residue-cleaned paired evaluation to `dev/TESTING.md`. Do not add exact reasoning-tag detection from this source alone.
- C07: Record the language, architecture, correlation, and sampled-condition limits in this card only; no product action.
- C08: If approved, extend H25 or add a linked detector/evaluator-architecture hypothesis in `dev/hypotheses.md`; no checker or judgement-record change.
- C09: Ruled 2026-07-26 (DR-66). The non-promotion held: the card's cited passive-voice direction was not used, and #68's direction comes from project measurement that contradicts it.
- C10: If approved, add direction-specific generator-transfer reporting to H25 in `dev/hypotheses.md` and the robustness lane in `dev/TESTING.md`; no checker change.
- C11: Ruled 2026-07-26 (DR-66). Values, wording inconsistency, and corrected null stay in this card; #68 rests on project measurement, not on this correlation.
- C12: Preserve semantic coherence and discourse structure as omitted explanatory dimensions in this card only; do not count `human-eyes/scripts/judgement.json` as detector-transfer coverage.

## Evaluation of approved changes

- C01: not applicable - recommendation pending; no product change implemented.
- C02: not applicable - recommendation pending; no product change implemented.
- C03: not applicable - recommendation pending; no product change implemented.
- C04: passed - 2026-07-26. `no-passive-voice-rate` (#68) flags 57% of the generated corpus and 29% of the human corpus at 5.0 per 1000 words; `no-it-pronoun-rate` (#69) flags 50% and 18% at 18.0; the #25 short-sentence branch flags 48% and 21% at 30.0. Verified against the live registry over 46 generated and 62 human documents of 300+ words. Held by the DR-66 block in `dev/evals/tests/test_grade.py` and calibrated in `dev/evals/xia-feature-rate-calibration-2026-07-26.md`.
- C05: not applicable - recommendation pending; no product change implemented.
- C06: not applicable - recommendation pending; no product change implemented.
- C07: not applicable - record-only recommendation pending; no product change implemented.
- C08: not applicable - recommendation pending; no product change implemented.
- C09: passed - 2026-07-26. No inherited feature-direction claim reached the product. The passive-voice direction in the catalogue and README comes from the corpus measurement recorded in `dev/evals/xia-feature-rate-calibration-2026-07-26.md`.
- C10: not applicable - recommendation pending; no product change implemented.
- C11: passed - 2026-07-26. The correlation values and their wording inconsistency remain in this card only; `human-eyes/scripts/grade.py` carries a measured frequency check, not this correlation.
- C12: not applicable - record-only recommendation pending; no product change implemented.

## Document review

- **Review status:** passed
- **Review method:** independent source-record reviewer: `/root/review_xia_generalization`
- **Findings resolved:** Extraction record revised to distinguish length-targeting from exact matching and add source selection plus 512-token truncation; correlation wording corrected to feature-gap shifts; Qwen/Solar-to-Llama transfer, passive-voice wording inconsistency and values, and omitted semantic/discourse dimensions added as C10-C12; project coverage remapped to `STATISTICAL_CHECKS`, `AGGREGATE_CHECKS`, and agent judgement records; cleaning coverage qualified; all recommendations now name exact affected files or record-only/no-action treatment
- **Unresolved findings:** none
