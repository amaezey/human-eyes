# theclaymethod/unslop

## Metadata

- **URL:** https://github.com/theclaymethod/unslop
- **Author / owner:** Clayton Kim / theclaymethod
- **Published:** initial commit 2026-02-08; reviewed revision committed 2026-07-07
- **Retrieved:** 2026-07-15
- **Extracted:** 2026-07-15
- **Source type:** first-party practitioner software repository, writing-pattern catalogue, and internal evaluation suite
- **Evidence tier:** Practitioner / teacher / editor essays; this is a practitioner tool with first-party tests and internal benchmarks, not independent validation of its pattern claims
- **Review mode:** new
- **Stable identifier:** commit 1e50bf273d59e6d98477005ec6a086643a6b27ea
- **Version / revision:** SKILL.md version 2.3.0 at commit 1e50bf273d59e6d98477005ec6a086643a6b27ea
- **Full-text status:** complete
- **Snapshot:** `snapshots/theclaymethod-unslop.md`
- **Extraction method:** Full Git clone over HTTPS, full-history fetch, `git archive`, and byte-preserving UTF-8 concatenation of all tracked files
- **Snapshot SHA-256:** `5eb1cd5d1cf2dc4ad61e48f6c1735f50a7b288131dd4a3a8b80f9a9a97e16d68`
- **Model / corpus scope:** English prose; 405 tracked files; 313 phrase entries, 77 structural patterns, and five silhouette metrics claimed in the README, although the pinned phrase scanner actually defines 82 structural regex entries; 479 deterministic script cases and 33 behavioural skill cases present at the pinned commit; silhouette separation corpus of eight human and twelve AI documents, with seven additional structure fixtures also used to build the human reference; recorded model-parity work dated 2026-07-06 and 2026-07-07 across named GPT, Claude, and open-weight models
- **Access limitations:** none for the repository text or pinned tree. The deterministic suite was rerun locally from the pinned archive with `python3 evals/run_adversarial.py`; it exited 1 after reporting 460 passes, one expected failure, and 18 regressions. Behavioural and live model-parity evaluations were not rerun because they require external model tools, including a prohibited Claude-backed path. Their results remain first-party reports. The local deterministic run used Python 3.9.6 on Darwin 25.5.0 arm64.

## Summary

`unslop` is an open-source agent skill and standard-library Python toolkit for detecting and rewriting English prose that carries machine-writing patterns. It combines a phrase scanner, a surface-structure scanner, a discourse-level silhouette scanner, guarded rewrite and suggestion flows, fact-preservation checks, voice profiling, and an eval-first contribution process. Much of its catalogue overlaps human-eyes. Its most distinct contribution is a five-metric attempt to score idea arrangement rather than isolated wording. That candidate is backed only by the repository's small internal corpus. The pinned checkout's official deterministic command also contradicts the README's test claim: it observed 460 passes, one expected failure, and 18 regressions across 479 cases, rather than the advertised 439 passes and one expected failure across 440 cases.

## Main insights

- The repository implements three deterministic layers: phrase matching, surface structure, and discourse silhouette. It presents detection as the trust-bearing part of the product and generation as subordinate to those gates.
- The README's advertised structural-pattern count is stale or otherwise unreconciled with the pinned implementation: it says 77, while `scripts/banned_phrase_scan.py` defines 82 structural regex entries. The separate surface-structure scanner exposes 16 document metrics.
- The phrase layer overlaps many live human-eyes checks, including significance inflation, negative parallelism, vague attribution, corporate language, filler, rhetorical questions, chatbot residue, generic conclusions, and promotional language.
- Context handling is a central design choice. The scanner masks quotations, blockquotes, and code fences by default, gates ambiguous vocabulary by collocation, and pairs target examples with literal or domain-specific controls.
- Those phrase controls are not complete. `FP-06` remains an expected failure for a literal place use of `delve into`, documenting a regex limit rather than a cleanly protected literal sense.
- The structure scanner measures sentence-length coefficient of variation, paragraph coefficient of variation, triad density, short one-line paragraph share, connective openers, signpost density, repeated sentence openers, participial closers, and conclusion codas. It supplies limited `docs` and `social` carve-outs.
- The silhouette scanner measures scaffold openers, cue-role entropy, intro-to-body preview fulfilment, early-to-late callback content, and heading preview. It compares a weighted composite with a committed human reference and flags at 1.0.
- The silhouette evidence is internal and small. The README reports 12 of 12 AI documents flagged and 0 of 8 human documents flagged. The reference comments say its thresholds were validated on those fixtures, not on independent, naturalistic, or human-eyes corpora.
- The source also preserves a silhouette null result: deleting discourse cues lets the dedicated silhouette scanner miss a rigid document, while the paired surface-structure scanner still flags repeated openers. The source therefore treats the two scanners as joint fences rather than claiming that silhouette is independently robust.
- The repository uses categorical language that exceeds its evidence, including hard or always-a-tell labels, a default-zero em dash rule, and claims that the em dash is the most reliable punctuation tell. Those claims do not supply independent rates or register-matched human controls.
- The official deterministic command at the pinned commit reported `PASS 460`, `XFAIL 1`, and `FAIL 18`, then exited 1. The README instead reports 440 cases with 439 passes and one expected failure.
- The 18 failures fall into four visible clusters: catalogue and kata coverage (`DOC-09`, `DOC-10`), harvest and encoding (`HARV-06`, `HARV-07`, `HARV-08`, `HARV-15`, `ENC-05`), the complete contribution flow (`CONTRIB-01` to `CONTRIB-10`), and traversal rejection (`SLUG-01`).
- Several local failures expose concrete compatibility or source defects. `harvest_samples.py` evaluates `int | float` inside `isinstance`, which fails under Python 3.9 despite the README claiming Python 3.8 or later. `contribute.py` has a nested-quote f-string that is a syntax error under Python 3.9. `check_pattern_coverage.py` attempts to treat an inline Python command as a path and raises `OSError: [Errno 63] File name too long`.
- The rewrite flow diagnoses before reconstruction, extracts constraints, runs scanners, and requires fresh validation. Human-eyes already takes the same general approach with a complete Audit and stricter source-bound preservation.
- The suggestion flow requires span-minimal, non-overlapping replacements that remain clean in context and pass preservation when applied together. Human-eyes already requires a fresh complete Audit of each changed context and retains the supporting artefacts.
- The voice system harvests user writing, excludes assistant turns, flags suspected contamination, derives a profile and voice card, uses held-out samples, and refuses to invent uncovered dimensions. This is more elaborate than human-eyes' current source-preservation guidance, but it also accepts any supplied style without rights or attestation checks.
- The contribution workflow starts from a wild specimen, requires a red false-negative row before implementation, adds a literal-use protection row, and asks for publication approval. This is relevant to H10's proposed false-positive intake, but the pinned contribution suite does not execute successfully in the observed environment.
- The source also defines an eight-part rewrite rubric with a 32/40 pass threshold. Its own unevaluated behavioural rows identify that the rubric can reward bland removal, erase qualified reasoning, and over-process very short text. `SKILL-RUBRIC-01` goes further by requiring a new named actor, number, or example, which conflicts with the source skill's separate instruction not to invent first-person experience, anecdotes, or unsupported certainty.
- The source is English-only. Its model-parity, behavioural, and structure-climb results are first-party reports tied to named models and dates. They were not independently reproduced in this review.

## Evidence and claims to extract

- **Direct source reviewed:** the complete 405-file tree at commit `1e50bf273d59e6d98477005ec6a086643a6b27ea`, including `README.md`, `SKILL.md`, all scanner implementations, the complete catalogue, all eval definitions and fixtures, command references, product doctrine, recorded decisions, and the pinned archive `snapshots/attachments/theclaymethod-unslop-1e50bf2.tar.gz`.
- **Method and sample:** first-party software implementation and internal evaluation artefacts. The pinned `evals/adversarial-evals.json` contains 479 script-target cases and 33 skill-target cases. The silhouette layer uses a committed reference and a reported validation set of eight human and twelve AI documents. The behavioural benchmark has 33 cases, split by the repository into tune, holdout, and holdback groups. Recorded live model comparisons are dated 2026-07-06 and 2026-07-07.
- **Direct versus cited evidence:** C01 to C12 and C14 are direct implementation, documentation, test-definition, or locally executed-suite observations from the repository. C13 records first-party model-run results that were read but not rerun. Background pattern claims inherited from Wikipedia and other sources remain indirect and cannot gain stronger evidence merely because `unslop` implements them.
- **Important limits and counterexamples:** most pattern families are not supported by independent prevalence or false-positive estimates; the silhouette corpus is small and internal; the phrase catalogue mixes candidate detection with categorical severity; genre handling is limited; the project is English-only; the README's structural-pattern and suite counts do not match the pinned implementation and eval file; the rewrite rubric conflicts with both its own behavioural safeguards and the skill's non-invention instruction; and the official deterministic suite exits unsuccessfully under the locally available Python version despite a stated Python 3.8 or later requirement.

## Skill-use audit

- **Good use:** compare concrete scanner mechanisms, contextual allowlists, literal controls, structural metrics, discourse-shape candidates, eval-first contribution practices, preservation checks, and suggestion-contract ideas with the live human-eyes implementation.
- **Misuse / overclaim:** treat `unslop`'s hard labels, zero-tolerance rules, test counts, internal separation, or model-tier conclusions as independent validation of a human-eyes rule, threshold, severity, or authorship inference.
- **Unsupported use:** claim generalisation beyond English, across untested genres or model versions, to naturalistic prevalence, or to individual-document authorship. The source cannot support adopting a checker merely because its own fixtures pass.
- **Underused evidence:** human-eyes has no direct equivalent to the five-part silhouette composite, per-author voice profiles, contamination-aware sample harvesting, or a specimen-to-publication contribution bundle. Each would require a separate product decision and project-specific evaluation.
- **Patterns left on the table:** callback-content recaps, preview fulfilment, heading preview, and discourse-role rotation are possible structure-research candidates. The source does not justify promoting them before matched human controls, held-out testing, register treatment, and false-positive review.

## Matched patterns / rules

- Phrase overlap: live implementations of `no-significance-inflation`, `no-negative-parallelisms`, `no-vague-attributions`, `no-corporate-ai-speak`, `no-filler-phrases`, `no-collaborative-artifacts`, `no-knowledge-cutoff-disclaimers`, `no-generic-conclusions`, and `no-copula-avoidance` recognise representative `unslop` constructions. `no-ai-vocabulary-clustering` provides density-level overlap. `no-rhetorical-questions`, `no-promotional-language`, `no-false-concession-hedges`, and `no-compound-modifier-density` provide only partial family overlap because their thresholds or accepted forms do not cover several exact source examples.
- Surface-structure overlap is partial and definition-sensitive. Focused live-check calls on the source fixtures confirmed `sentence-length-variance` on `struct01_uniform_essay.md`, `no-staccato-sequences` on `struct07_staccato.md`, `no-anaphora` on `struct13_opener_repetition.md`, `no-superficial-ing` on `struct14_participial.md`, and general list-density coverage on `struct05_bold_listicle.md`. The similarly named `paragraph-length-uniformity`, the then-separate #10a density check (retired 2026-07-25 via DR-19G), `no-formulaic-openers`, `no-generic-conclusions`, and `no-tidy-paragraph-endings` checks did not cover the corresponding source fixtures in this focused run; `no-section-scaffolding` and `overall-signal-stacking` remain conceptually adjacent rather than equivalents.
- Manual and process overlap: `structural_monotony`, `tonal_uniformity`, `genre_specific`, and `formulaic_parallelism` in `human-eyes/scripts/judgement.json`; structure repair, source preservation, and fresh-audit requirements in `human-eyes/SKILL.md`, `human-eyes/references/process.md`, and `human-eyes/references/voice.md`.
- Distinct candidate family: no live human-eyes check directly computes `scaffold_opener_share`, `role_entropy_bits`, `preview_fulfillment`, `callback_content`, `heading_preview`, or `silhouette_penalty`.
- Severity challenge: human-eyes uses contextual severities and a non-authorship boundary. It does not support importing `unslop`'s hard or always-a-tell labels from this source alone.
- Rewrite-evaluation challenge: human-eyes has no 32/40 quality score, treats the source as a closed factual record, forbids invented specifics, and tells reports not to add a score or fixed-count checklist. This conflicts with `unslop`'s rubric and its `SKILL-RUBRIC-01` requirement for a new named actor, number, or example.

## Associated hypotheses

- H1, continuous calibrated register-distance score per pattern.
- H3, drop detection framing entirely.
- H7, five-check gating plus advisory catalogue.
- H9, similar-species disambiguation per pattern.
- H10, user-reported false-positive intake.
- H12, genre-aware threshold calibration.
- H17, calibration golden set gating grader changes.
- H20, severity calibration of agent-judgement items.
- H25, model-family versus generic-AI residue.
- Proposed follow-up: evaluate discourse-silhouette metrics as separate, interpretable candidates before considering any aggregate score or active rule.

## Questions / follow-up

- Should the five silhouette metrics enter a human-eyes research evaluation using the established paired corpus and a broader neutral, register-matched human corpus?
- Should any `unslop` examples be added as external regression candidates after deduplication and licensing review, without importing its severities or thresholds?
- Should H10's future intake design borrow the exact-specimen, red-first, literal-control, redaction, and publication-confirmation structure?
- Should contamination-aware voice harvesting or per-author profiles remain outside the current human-eyes product boundary?
- Should the repository's stale README count and failing deterministic run be recorded only as source limitations, or should Mae request a later refresh after upstream fixes?
- Should `unslop`'s 32/40 rewrite rubric be recorded as an explicit non-promotion because its authenticity criterion and behavioural row conflict with human-eyes' closed-source, no-invention, and no-score rules?
- No additional source access is required. External model-backed results would require a separately authorised reproduction protocol and are not necessary to complete this source card.

## Update provenance

Not applicable: initial ingestion.

## Decision history

None: initial review.

## Project coverage

This is the authoritative review table. Every recommendation remains pending for Mae. No product rule, severity, threshold, pattern, hypothesis, or process file was changed.

| Source claim or example | Evidence assessment | Existing project coverage | Missing or challenged | Recommendation | User decision | Implementation status |
|---|---|---|---|---|---|---|
| C01: `unslop` implements three deterministic layers with generation gated by detection. Its README claims 313 phrase entries, 77 structural patterns, and five discourse-silhouette metrics, but the pinned phrase scanner contains 313 literal entries and 82 structural regex entries, while the surface-structure scanner exposes 16 document metrics. | Direct first-party README, scanner code, and eval files at the pinned commit. The five silhouette metrics and 313 literal entries match the implementation; the advertised count of 77 structural patterns does not. These counts describe repository mechanisms, not independent evidence that each is valid. | Partly covered: human-eyes has 50 deterministic records, 15 agent-judgement records, `overall-signal-stacking`, and complete Audit requirements across phrasing, structure, and voice. | human-eyes does not use the same three-layer architecture or a separate silhouette composite. The source's counts are internally inconsistent and are not comparable one-for-one with human-eyes records. | Record `unslop` as practitioner prior art. If Mae approves a comparison exercise, map mechanisms rather than counts, preserve the count discrepancy, and require human-eyes-specific evidence before any product change. | pending | not started |
| C02: The phrase scanner uses collocation gates, quote and code masking, false-negative examples, literal false-positive controls, and recall rows across a large catalogue of AI-associated phrases. | Direct scanner, catalogue, and eval evidence. It establishes implemented behaviour for the pinned code, subject to the failing suite. The expected-failure row `FP-06` documents that a literal place use of `delve into` still collides with the target regex. The source does not establish population prevalence or universal severity. | Partly covered: direct calls to the live checker implementations confirm representative overlap for `no-significance-inflation`, `no-negative-parallelisms`, `no-vague-attributions`, `no-corporate-ai-speak`, `no-filler-phrases`, `no-collaborative-artifacts`, `no-generic-conclusions`, and `no-copula-avoidance`; `no-ai-vocabulary-clustering` covers density rather than the same catalogue. A single source-style question is recognised by `no-rhetorical-questions` but remains below its two-candidate threshold. | Exact variants, masking rules, allowlists, thresholds, and residual false-positive boundaries differ. `world-class` and `state-of-the-art` do not trigger `no-promotional-language`; they only contribute to `no-compound-modifier-density`, which requires three stock compounds in one sentence. `a hidden gem` is not recognised by either check. The source's `While X is promising, Y remains a challenge` example is not recognised by `no-false-concession-hedges`. A surface-only human-eyes run on the `unslop` README produced 13 findings but was incomplete and visibly confounded by documentation formatting and quoted examples, so it is not a complete coverage result. | If Mae approves, deduplicate selected external examples against the current robustness seed, then test each candidate with legitimate controls and complete project reporting. Do not bulk-import the catalogue. | pending | not started |
| C03: The structure scanner operationalises sentence and paragraph variation, triads, one-line staccato, connective and repeated openers, signpost density, participial closers, and recap codas with explicit thresholds and limited genre suppressions. | Direct implementation and paired fixtures. Thresholds are project-chosen and internally tested, not independently calibrated across genres. | Partly covered: focused calls to live human-eyes implementations confirmed `sentence-length-variance` on the source's uniform-rhythm fixture, `no-staccato-sequences` on its staccato fixture, `no-anaphora` on its repeated-opener fixture, `no-superficial-ing` on its participial-closer fixture, and `no-excessive-lists` on its bold-listicle fixture. The projects use different calculations and thresholds, so these are mechanism-level overlaps rather than equivalent checks. | The source's connective and signpost fixtures triggered no corresponding human-eyes finding, and its coda fixture did not trigger `no-generic-conclusions` or `no-tidy-paragraph-endings`. Its parallel-enumeration and uniform-paragraph fixtures did not trigger the then-separate #10a density check (retired 2026-07-25 via DR-19G) or `paragraph-length-uniformity`. The bold-listicle fixture was caught only as general list density, not as the same formatting pattern. human-eyes also does not compute the source's signpost-density, opener-share, opener-run, or participial-closer ratios. | If Mae approves, perform a definition-level comparison for the unmatched mechanisms and ratios, then test them on matched human controls and held-out style gates before deciding whether any existing check needs extension. | pending | not started |
| C04: The silhouette layer scores scaffold openers, cue-role entropy, preview fulfilment, callback content, and heading preview against a human reference, with a composite threshold of 1.0; a documented cue-deletion attack evades this scanner but remains visible to the paired surface-structure scanner. | Direct code, reference fixture, separation gate, and paired `SIL-05` and `SIL-05b` eval rows. The source reports 12 of 12 AI documents flagged and 0 of 8 human documents flagged. This is a small, internal, apparently development-exposed corpus with no uncertainty interval or independent replication, and the preserved evasion shows that the silhouette layer is not sufficient alone. | Partly covered: `structural_monotony`, `formulaic_parallelism`, `no-section-scaffolding`, `no-tidy-paragraph-endings`, `no-generic-conclusions`, and the structure-repair guidance address related readings without these metrics. | No live check measures intro-to-body fulfilment, early-to-late callback after middle absence, or discourse-role entropy. The source does not establish transfer, genre calibration, or false-positive behaviour on human-eyes corpora. Its own null result also supports evaluating the metrics separately rather than treating the composite as a standalone detector. | If Mae approves, open a research-only evaluation of the five metrics as separate candidates. Use the established paired corpus plus broader neutral human controls, keep individual metrics visible, test cue-deletion and other evasion controls, and do not begin with the composite threshold. | pending | not started |
| C05: The catalogue assigns hard and soft statuses and makes categorical claims, including a default-zero em dash policy and the claim that em dash overuse is the most reliable punctuation tell. | Direct first-party guidance and scanner policy. No independent rates, model-date comparison, register-matched human baseline, or causal evidence supports the categorical wording. | Challenges current behaviour: human-eyes treats `no-em-dashes` as a strong warning, preserves deliberate source style where allowed, considers genre and context, and states that patterns do not prove authorship. | Importing `unslop`'s hard status or paragraph threshold would override current project policy without adequate evidence. | If Mae approves, record an explicit non-promotion: do not use this source to change `no-em-dashes`, any severity, or the project's non-authorship framing. Any future change requires separate evidence and project evaluation. | pending | not started |
| C06: The source supplies `docs` and `social` carve-outs, literal and quoted-use protections, and an English-only decline. | Direct scanner and product-doctrine behaviour. Genre support is narrow and manually selected. The English-only bound is explicit. | Partly covered: `genre_specific`, `STRATEGY.md` treatment-in-context requirements, quoted-evidence handling, protected literals, and `dev/TESTING.md` register controls provide broader principles. | human-eyes does not encode the same two genre switches, and neither project has evidence here for multilingual transfer. | If Mae approves, use the source only as another argument for H12 evaluation. Do not copy its genre suppressions or language policy without user and product decisions. | pending | not started |
| C07: The contribution method requires an exact wild specimen, a red false-negative row before implementation, a literal-use protection row, no silent grandfathering, redaction discipline, and publication confirmation. | Direct contribution docs, scripts, issue template, and eval definitions. The design is concrete, but its ten `CONTRIB` cases fail in the observed pinned run because `contribute.py` does not parse under local Python 3.9. | Partly covered: H10 proposes opt-in false-positive intake; `dev/TESTING.md` requires legitimate near-match preservation and provenance. No equivalent public contribution bundle exists. | H10 is about false positives, while this flow focuses new AI-ism specimens. Licensing, consent, privacy, and the currently failing implementation remain unresolved. | If Mae approves, borrow only the intake shape for a future H10 or pattern-reporting design: exact specimen, context, explicit consent, redaction that preserves the target, and a matched control. Do not adopt the code or workflow as-is. | pending | not started |
| C08: At commit `1e50bf2`, the README claims 440 deterministic cases with 439 passes and one expected failure, but `evals/adversarial-evals.json` contains 479 script cases and the official local command observed 460 passes, one expected failure, and 18 regressions, exiting 1. | Direct README and eval-file comparison plus reproduced run on Python 3.9.6, Darwin 25.5.0 arm64. Failure IDs were `DOC-09`, `DOC-10`, `HARV-06`, `HARV-07`, `HARV-08`, `HARV-15`, `CONTRIB-01` to `CONTRIB-10`, `ENC-05`, and `SLUG-01`. The source itself labels ordinary failures as regressions. | Fully covered as an evidence-handling principle: `dev/TESTING.md` distinguishes incomplete diagnostics from benchmark results, requires exact refs, and preserves weak or failed cases. | The source's advertised count and claims that coverage is structurally enforced are not confirmed by the pinned checkout in this environment. Some failures reflect incompatibility with the claimed Python 3.8 or later floor; others expose direct coverage-command defects. | Record the reproduced result and treat README metrics as unverified or stale. If Mae approves a future refresh, rerun the same pinned command and compare with a newer upstream commit without replacing this historical observation. | pending | not started |
| C09: Rewrite diagnoses first, extracts constraints, reconstructs under register guards, reruns scanners, and uses targeted structure directives rather than accepting a model's self-check. | Direct command docs, pipeline, preservation code, and internal evals. The general process is first-party engineering evidence; model-dependent effectiveness remains internally reported. | Fully covered and stricter: `human-eyes/SKILL.md` requires a complete source Audit, protected literals, a new work bundle, a fresh complete output Audit, regression and oscillation handling, and no more than three passes. | `unslop` uses deterministic scanners where human-eyes requires deterministic plus agent-assessed coverage. Importing its flow would weaken the complete-Audit boundary. | If Mae approves, record no product change. Retain the current human-eyes workflow and use `unslop` only as corroborating practitioner process evidence. | pending | not started |
| C10: Fact preservation protects numbers, names, dates, units, quotes, URLs, negation, modality, conditionals, and scope, while requiring manual rereading because scanners are insufficient. | Direct preservation reference, scripts, tests, and explicit limitations. Some exact rules are simplistic, and the suite contains historical regression cases showing why automated constraint checks are incomplete. | Fully covered and broader: `human-eyes/references/process.md` protects argument, facts, examples, stance, genre, uncertainty, quotations, citations, names, dates, links, quantities, and complete qualified sentences, while treating the source as a closed factual record. | No substantive gap established by this source. | If Mae approves, record no product change. Keep human-eyes' closed-source and complete-sentence protections rather than replacing them with `unslop`'s extractor rules. | pending | not started |
| C11: Cleanup emits reviewable, non-overlapping, span-minimal suggestions and requires replacement scanning, accept-all cleanliness, and preservation. | Direct command docs, suggestion code, and eval rows. These tests are first-party contract evidence, not proof of user-facing safety in all contexts. | Fully covered and stricter: human-eyes applies each replacement to its full surrounding context, creates a fresh work bundle, completes all agent-assessed records, runs a fresh complete Audit, and retains three proof artefacts. | `unslop`'s isolated replacement scanner and deterministic accept-all gate do not meet the human-eyes complete-Audit requirement by themselves. | If Mae approves, record no product change. Do not weaken changed-context validation or substitute deterministic-only acceptance. | pending | not started |
| C12: Teach and mimic exclude assistant-authored transcript turns, flag suspected AI contamination, build a per-author profile and layered voice card, use held-out acceptance data, surface uncovered dimensions, and reject unsupported profile claims. | Direct product doctrine, scripts, fixtures, and internal tests. Voice corpora are supplied or synthetic, and the repository explicitly has no rights or attestation machinery for mimicry samples. | Partly covered: `human-eyes/references/voice.md` preserves source voice, forbids fabricated personality, and requires source-grounded details. human-eyes does not harvest or fit a reusable author profile. | Per-author profiling, contamination detection, consent, provenance, rights, privacy, and reward-gaming controls are outside the present human-eyes editing boundary. | Mae must decide whether reusable author profiles belong in scope. Until then, record the method as adjacent prior art and make no voice-harvesting or mimicry change. | pending | not started |
| C13: Recorded first-party model runs say cheap tiers can handle span replacement, full register-sensitive rewrites need stronger models, all six tested models failed one-pass macro structure, and scanner-directed climbs sometimes recover it. | Direct repository reports dated 2026-07-06 and 2026-07-07, not rerun here. The records mix model families, tasks, small case counts, and version-specific behaviour. Claude-backed reproduction was prohibited for this task. | Partly covered: `dev/TESTING.md` has model-backed lifecycle tests, exact-version provenance requirements, and release gates for preservation and convergence. H25 tracks model-family versus generic residue. | These reports do not validate a standing human-eyes model-routing rule, and model behaviour can drift. | If Mae approves, keep the findings as dated, indirect process evidence only. Any routing change requires a separately authorised, current, human-eyes-specific parity evaluation across the intended executors. | pending | not started |
| C14: `unslop` scores rewrites on eight one-to-five criteria and sets a 32/40 pass threshold, while its own behavioural rows warn that the rubric can reward bland removal, erase qualified reasoning, and over-process short text; `SKILL-RUBRIC-01` nevertheless requires adding a named actor, number, or example absent from its prompt. | Direct first-party rubric, skill instruction, and behavioural eval definitions. The failure modes are source-authored test intentions, not independently rerun model results. The requirement to add an unsupported specific conflicts with the source's separate instruction not to invent first-person experience, anecdotes, or unsupported certainty. | Challenges current behaviour: `human-eyes/SKILL.md` and `human-eyes/references/process.md` treat the source as a closed factual record, forbid invented details, require protected-literal checks, and explicitly say not to add a score or fixed-count checklist. | Importing the 32/40 threshold, authenticity criterion, or new-specific requirement would weaken source fidelity and introduce an uncalibrated aggregate score. | If Mae approves, record an explicit non-promotion of the rubric, its threshold, and its unsupported-specific requirement. Retain human-eyes' claim-traceability, complete-Audit, and no-score requirements. | pending | not started |

## Recommendations

- C01: Record `unslop` as practitioner prior art and its advertised structural-count discrepancy; only if Mae approves, compare mechanisms rather than raw catalogue counts.
- C02: If approved, deduplicate selected phrase examples against current robustness fixtures and test them with legitimate controls; do not bulk-import the catalogue.
- C03: If approved, compare unmatched structure-ratio definitions and evaluate them on matched controls before extending any check.
- C04: If approved, evaluate the five silhouette metrics separately on established and broader neutral corpora, including cue-deletion controls, before considering an aggregate score.
- C05: If approved, record an explicit non-promotion of `unslop`'s categorical severities and em dash policy.
- C06: If approved, use the source as supporting rationale for H12 research only; copy no genre or language rule directly.
- C07: If approved, adapt the exact-specimen and consent shape for a future H10 or pattern-reporting design, not the failing implementation.
- C08: Record the reproduced suite result and treat README metrics as stale or unverified; refresh only if Mae requests a later version comparison.
- C09: If approved, record no product change because human-eyes already has a stricter audit-first rewrite contract.
- C10: If approved, record no product change because current source-preservation guidance is broader.
- C11: If approved, record no product change and retain complete changed-context Audits.
- C12: Mae must decide whether reusable author profiles belong in scope; make no harvesting or mimicry change while pending.
- C13: Keep the model-tier findings dated and indirect; require separately authorised current parity work before any routing change.
- C14: If approved, record an explicit non-promotion of the 32/40 rubric and unsupported-specific requirement; retain human-eyes' closed-source and no-score rules.

## Evaluation of approved changes

- C01: not applicable - recommendation pending; no product change implemented.
- C02: not applicable - recommendation pending; no product change implemented.
- C03: not applicable - recommendation pending; no product change implemented.
- C04: not applicable - recommendation pending; no product change implemented.
- C05: not applicable - recommendation pending; no product change implemented.
- C06: not applicable - recommendation pending; no product change implemented.
- C07: not applicable - recommendation pending; no product change implemented.
- C08: not applicable - record-only recommendation pending; no product change implemented.
- C09: not applicable - record-only recommendation pending; no product change implemented.
- C10: not applicable - record-only recommendation pending; no product change implemented.
- C11: not applicable - record-only recommendation pending; no product change implemented.
- C12: not applicable - recommendation pending; no product change implemented.
- C13: not applicable - recommendation pending; no product change implemented.
- C14: not applicable - recommendation pending; no product change implemented.

## Document review

- **Review status:** passed
- **Review method:** independent source-record reviewer: Codex CLI fresh process, did not perform the extraction
- **Findings resolved:** corrected C01 and related scope text so the README's claimed 77 structural patterns are not presented as an implemented count when the pinned phrase scanner defines 82 structural regex entries; added the omitted `FP-06` phrase-control limit and silhouette cue-deletion null result to C02 and C04; replaced title-level phrase-family and structure mappings with focused live-check results and exact uncovered examples; added C14 for the omitted rewrite rubric, its self-identified failure modes, and its unsupported-specific contradiction; independently confirmed the snapshot and attachment hashes, embedded and upstream commit identity, 405-file snapshot completeness, and the pinned eval result of 460 passes, one expected failure, and 18 regressions
- **Unresolved findings:** none
