# A Systematic Analysis of Linguistic Features in AI-Generated Text Detection Across Domains and Models

## Metadata

- **URL:** https://arxiv.org/abs/2606.04177
- **Author / owner:** Yassir El Attar, Esra Dönmez, Maximilian Maurer, and Agnieszka Falenska
- **Published:** 2026-06-02
- **Retrieved:** 2026-07-15
- **Extracted:** 2026-07-15
- **Source type:** academic preprint with large-scale empirical corpus analysis
- **Evidence tier:** Peer-reviewed / academic empirical; this version is a preprint and is not identified as peer reviewed
- **Review mode:** new
- **Stable identifier:** arXiv:2606.04177v1; DOI 10.48550/arXiv.2606.04177
- **Version / revision:** v1, submitted 2026-06-02
- **Full-text status:** complete
- **Snapshot:** `snapshots/el-attar-linguistic-features-ai-text-detection.md`
- **Extraction method:** official arXiv v1 PDF downloaded and preserved; all 30 pages extracted from the embedded text layer with `pdftotext -layout`; pages 1, 15, 24, 25, and 30 rendered and visually checked
- **Snapshot SHA-256:** `c554e78921648b6a498fa559a9c65818355eed1bb42c6b1acb4bad1125abdc07`
- **Model / corpus scope:** English MAGE benchmark continuations from 27 models in seven families (OpenAI GPT, LLaMA, GLM-130B, FLAN-T5, OPT, BigScience, and EleutherAI) across ten domains, with a GPT-4 test set from four additional domains; 284 interpretable linguistic features; separate ChangeMyView data from GPT, LLaMA, and Mistral for a prompt and model-alignment pilot
- **Access limitations:** none for full text; arXiv HTML returned 404, so the official PDF was used. Multi-column text and chart labels are mechanically interleaved in places, and some symbol fonts are transformed. The preserved PDF is authoritative for layout and figures.
- **Queue title note:** the user queue title, "A Systematic Analysis of Linguistic Features", is an abbreviated form. The authoritative URL carries the longer title used for this card.

## Summary

This arXiv preprint tests whether 284 interpretable linguistic features can distinguish human and AI-generated English text across models and domains. It trains linear SVM classifiers on the MAGE benchmark, evaluates eight in-domain and out-of-domain testbeds, and uses feature-area ablations to study robustness. Its strongest contribution to human-eyes is twofold. It reinforces the need for model- and domain-aware evaluation, and it directly challenges the live `vocabulary-diversity` check's one-way assumption that AI prose has lower type-token ratio. Lexical-richness features are highly informative as a group, but their direction, usefulness, and interaction with other features vary with text length, domain, model family, and evaluation setting.

## Main insights

- A linear classifier using only linguistic features achieves 0.827 Macro F1 in the mixed in-domain setting and 0.808 on the four-domain GPT-4 test, but falls to 0.588 on unseen text-domain and model-family pairs. Aggregate success therefore hides substantial transfer failures.
- Model-family and text-domain effects are large. In the most difficult paired transfer test, Macro F1 ranges from 0.345 to 0.889 across the 70 held-out domain-model combinations.
- Appendix E reports near-zero pairwise TTR correlations across model families. Those results show weak co-variation across domains, but they do not by themselves establish distance or clustering between family distributions. The paper uses the same near-zero pattern to support both distinct family regions and similar LLaMA-OpenAI patterns, so that interpretation needs caution.
- Lexical richness is the most consistently important feature area. Removing its three features drops Macro F1 from 0.827 to 0.696 in the mixed in-domain test and from 0.808 to 0.531 in the four-domain GPT-4 test.
- The paper's TTR results challenge the current project direction. Its aggregate plots place human text around 0.6 and AI text around 0.8, and its three XSum examples report human TTR from 0.271 to 0.292 versus AI TTR from 0.558 to 0.694. The live check flags only low TTR.
- The paper also contains an internal contradiction: it says humans have higher values across all three lexical-richness features and calls that greater diversity, then immediately reports higher TTR for AI. Its figures and qualitative examples support the higher-AI-TTR direction, while hapax counts are higher for the much longer human passages.
- Length is a major confound in the selected XSum examples. Human passages contain 822 to 1,191 tokens while AI continuations contain 108 to 190, so raw TTR and absolute hapax counts cannot be imported as fixed document thresholds.
- Lexical-only classifiers can outperform the full 284-feature set in some out-of-domain settings, yet they underperform the full set on nearly all matched in-domain domain-model pairs. Other feature groups sometimes add signal and sometimes add noise.
- SQuAD is an important counterexample: removing lexical richness barely changes or slightly improves performance in several settings. DialogSum is another, with morphology and information features actively reducing performance while semantic features help.
- A small ChangeMyView pilot suggests model-family alignment matters more than prompt formulation. The reverse cross-dataset Macro F1 rises from 0.435 to 0.850 when training and test data are restricted to shared OpenAI and LLaMA families. This is a limited pilot, not a general prompt-effect estimate.
- The source is English-only, excludes the newest models, does not factorially test prompt variation in the main MAGE experiments, and relies on one feature toolkit plus linear SVMs. The findings are aggregate classifier evidence, not individual-document authorship rules.
- Background claims about prior stylometric cues remain cited evidence unless this paper measures them directly. The ablation results support feature groups and condition dependence, not universal directions for every surface, syntactic, emotion, readability, or psycholinguistic feature.
- The paper discloses GitHub Copilot use for code completion and Grammarly use for spelling and grammar correction. This is provenance information, not evidence that the paper's prose or results are AI-generated.

## Evidence and claims to extract

- **Direct source reviewed:** arXiv:2606.04177v1, all 30 pages, including methods, eight testbeds, full results, limitations, ethical considerations, references, appendices A to F, 16 tables, 22 figures, qualitative examples, and the AI-assistance disclosure.
- **Method and sample:** MAGE continuation-prompt data contains 93,318 human and 194,839 AI training instances, 28,799 human and 24,151 AI validation instances, and 28,741 human and 24,226 AI test instances across ten English domains. The paper evaluates 27 models in seven families and a separate GPT-4 test over CNN/DailyMail, DialogSum, IMDb, and PubMed. It extracts 284 features in 11 areas, standardises them, trains class-weighted linear SVMs, and reports Macro F1, AUROC, and average recall. Feature importance is studied with leave-one-area-out and cumulative ablation. A separate ChangeMyView corpus contains 157,880 human comments and roughly 13,500 outputs from each of GPT, LLaMA, and Mistral.
- **Direct versus cited evidence:** C01 to C12 below are direct design, result, limitation, inconsistency, ethics, or provenance claims from this paper. C13 records related-work claims inherited from cited studies and does not treat them as direct evidence.
- **Important limits and counterexamples:** unseen paired transfer reaches 0.345 Macro F1 in the weakest cell; SQuAD shows little lexical-richness dependence; DialogSum benefits from removing some feature groups; lexical-only models reverse from harmful in matched in-domain evaluation to helpful in some out-of-domain settings; TTR direction is internally described inconsistently; selected human and AI passages differ sharply in length; prompt variation is only piloted on ChangeMyView; the benchmark is English-only and does not include the newest models; near-zero TTR correlations are overinterpreted as feature-space separation and similarity; results are correlational classifier and ablation evidence, not causal proof or individual-document diagnosis.

## Skill-use audit

- **Good use:** Support condition-aware evaluation of distributional prose features; justify model-family, domain, prompt, length, and corpus metadata; strengthen the case for direct human controls; reevaluate the direction and threshold of `vocabulary-diversity`; preserve nulls, reversals, and weak transfer cells.
- **Misuse / overclaim:** Treating lexical richness, TTR, hapax counts, emotion, morphology, or any single feature as a universal AI tell; importing the paper's classifier scores as human-eyes thresholds; calling a document AI-generated because a feature matches the aggregate distribution.
- **Unsupported use:** Claims about non-English writing, current model families outside the benchmark, mixed human-AI documents, naturalistic prevalence, causal effects of prompts, or exact product behaviour without a human-eyes-specific matched evaluation.
- **Underused evidence:** The project has a coarse TTR check and open genre/model hypotheses, but it does not evaluate multiple lexical-richness measures jointly, control TTR for length, report condition-specific direction reversals, or run a factorial domain-model feature-ablation lane.
- **Patterns left on the table:** Length-adjusted lexical richness, hapax behaviour, lexical density, explicit feature-direction checks, and condition-specific ablation are research candidates. The source argues against promoting them as fixed universal checks before matched controls and stability testing.

## Matched patterns / rules

- `vocabulary-diversity` / pattern #53 in `human-eyes/scripts/grade.py`, `human-eyes/scripts/patterns.json`, and `human-eyes/references/patterns.md`: direct conceptual match for TTR, but the paper challenges the fixed low-TTR direction and exposes length, domain, and model dependence.
- `sentence-length-variance` / pattern #52 and `paragraph-length-uniformity`: members of the broader surface and structural feature family, but the paper does not validate their live thresholds and often finds non-lexical areas condition-dependent or noisy.
- `genre_specific` and `even_jargon_distribution` in `human-eyes/scripts/judgement.json`: partial context coverage for genre and within-document terminology distribution; neither supplies the paper's domain-model ablation evidence.
- `overall-signal-stacking`: conceptually compatible with multifeature evidence, but the paper's SVM and ablation results do not validate the project's pattern weights or document-level aggregate threshold.
- `STRATEGY.md`: fully aligned on treating patterns as inspectable writing evidence rather than authorship classification.

## Associated hypotheses

- H1, continuous calibrated register-distance score per pattern.
- H3, drop detection framing entirely.
- H12, genre-aware threshold calibration.
- H22, long-tail compression and grammatical standardisation.
- H24, register-specific vocabulary density.
- H25, model-family versus generic-AI residue.
- Proposed follow-up: length-adjusted lexical richness should be evaluated as a multidimensional, condition-specific feature family before any fixed direction or threshold is retained.

## Questions / follow-up

- Should `vocabulary-diversity` remain active while its direction and 0.40 threshold are reevaluated on length- and register-matched human and AI samples?
- Should future robustness work extend the already pending prompt-model-domain lane with feature-area ablations and explicit direction-reversal reporting?
- Should source records gain a standard field for disclosed editing, coding, translation, or grammar-assistance tools, without treating disclosure as authorship evidence?
- No additional source access is required for this card. Direct review of cited background studies remains necessary before their individual feature-direction claims are promoted.

## Update provenance

Not applicable: initial ingestion.

## Decision history

None: initial review.

- C06 approved 2026-07-17: flip #53's direction; implementation is pending a plan, so the status stays `not started`. All other rows remain pending, including C05's broader lexical-richness reevaluation.

## Project coverage

This is the authoritative review table. Give every relevant source claim or example a stable claim ID in the first cell.

Escape any literal pipe inside any table cell as `\|`; an unescaped extra pipe makes the row invalid.

| Source claim or example | Evidence assessment | Existing project coverage | Missing or challenged | Recommendation | User decision | Implementation status |
|---|---|---|---|---|---|---|
| C01: The study evaluates 284 interpretable features across 27 LLMs, ten MAGE domains, four additional GPT-4 domains, and eight testbeds, using class-weighted linear SVMs plus feature-area ablations. | Direct preprint method and sample; 394,074 listed MAGE train, validation, and test instances before the separate GPT-4 set; English continuation data; controlled benchmark rather than naturalistic prevalence. | Partly covered: `dev/TESTING.md` requires matched human and AI samples, broader genres and registers, prompt provenance, length controls, version refs, and weak-case reporting. The pending 2026 detector-research section in `pattern-opportunities.md` already records a factorial prompt-model-domain lane from Xia, Stańczak, and Roth. | No current human-eyes evaluation combines domain-model transfer with feature-area ablation, and current corpus size cannot support 284-feature inference. | If approved, extend the existing pending robustness-lane proposal in `dev/TESTING.md` and `pattern-opportunities.md` with a small predeclared feature-area ablation matrix. Keep it separate from the ordinary literal-first comparison corpus and report sample construction, length, model, prompt, domain, and evaluator version. | pending | not started |
| C02: Linguistic-feature classifiers score 0.827 Macro F1 in mixed in-domain data and 0.808 on four unseen GPT-4 domains, but only 0.588 on 70 held-out domain-model pairs, with cells from 0.345 to 0.889. | Direct aggregate and per-cell classifier results. AUROC remains high in some weak-F1 settings, so threshold-independent and class-balanced metrics tell different stories. These scores are benchmark-specific and do not measure human-eyes catalogue quality. | Partly covered: `STRATEGY.md` rejects authorship verdicts; `dev/TESTING.md` requires reversed and weak pairs, per-check evidence, and a non-classification statement; H1 and H12 anticipate calibrated, register-specific evidence. | Current reports do not include controlled domain-model transfer or preserve both Macro F1 and AUROC-style discrimination because human-eyes is not an authorship classifier. | Record the transfer gap as evaluation evidence. If approved, require condition-specific weak-case and uncertainty reporting in the pending robustness lane. Do not import these scores as product thresholds or an authorship score. | pending | not started |
| C03: Model families carry different linguistic signatures. In model-held-out tests, mean Macro F1 falls from 0.735 to 0.609, with OpenAI the only family improving, while the 70-pair test ranges from 0.481 mean F1 for GLM to 0.684 for OPT. Appendix E reports near-zero pairwise TTR correlations across model families. | Direct model-domain results and direct correlation results. Family-level aggregation reduces individual-model noise but can hide variation. Near-zero correlations show weak co-variation across domains, not distance or clustering in feature space. The authors use them to support both distinct family regions and similar LLaMA-OpenAI patterns, so that interpretation is internally strained. The OpenAI improvement is an observed transfer result, not proof of a shared causal feature. | Partly covered: H25 in `dev/hypotheses.md` distinguishes model-family residue from generic AI evidence, and source-card metadata records model scope. | H25 does not yet require direction-specific train-family to test-family transfer or feature-area reversals. The live checks have no model-family conditioning. | If approved, extend the pending H25 robustness work to report each train-family and held-out-family direction, with model versions and dates. Preserve correlation as co-variation evidence rather than distance evidence. Do not make live checker thresholds model-specific from this source alone. | pending | not started |
| C04: Text domains also change detectability. Mean Macro F1 falls from 0.904 in fixed-domain evaluation to 0.794 when domains are held out; ROCStories, HellaSwag, and TLDR drop most, while SciGen slightly improves. | Direct text-domain results across ten MAGE domains. Appendix E adds descriptive Wasserstein-distance clustering over the ten most discriminative features, but it does not causally establish why F1 changes. Domain categories are author interpretations based on benchmark sources, and each source has distinct length and task properties. | Partly covered: H12 proposes genre-aware threshold calibration; `genre_specific` in `judgement.json` branches by broad genre; `dev/TESTING.md` requires varied genres and register-aware interpretation. | The live genre branch is a manual watchlist, not a calibrated domain baseline. It does not represent benchmark-specific task, length, or source distributions. | If approved, add domain and register as mandatory grouping fields in the pending robustness lane, with results reported per domain rather than only averaged. Do not map MAGE dataset names directly to live genre thresholds. | pending | not started |
| C05: Lexical richness is the most consistently important feature area. Removing TTR, the token-normalised global token hapax measure, and lexical density drops Macro F1 from 0.827 to 0.696 in TB4 and from 0.808 to 0.531 in TB7; other feature-area removals are much smaller or sometimes helpful. | Direct leave-one-area-out ablation. Under the paper's stated rule that count-based features outside named raw-count exceptions are normalised by token count, the classifier's global hapax feature appears token-normalised; the qualitative examples display absolute counts. The result establishes group importance for these classifiers, not a universal direction for each metric. | Partly covered: `vocabulary-diversity` in `grade.py` computes document TTR; pattern #53 names vocabulary diversity; H1, H12, and H24 anticipate calibrated, register-aware distributional evidence. | The live project measures only raw TTR with a fixed low-direction threshold. It does not measure a token-normalised hapax feature or lexical density, adjust TTR for length, or test whether the feature helps across conditions. | If approved, open a focused reevaluation of pattern #53 using length- and register-matched human controls, multiple lexical-richness measures, held-out model/domain conditions, and uncertainty. Do not add hapax or lexical-density checks before that evaluation. | pending | not started |
| C06: The reported TTR direction challenges the live check. Aggregate figures place humans near 0.6 and AI near 0.8, and three XSum pairs report human TTR of 0.271 to 0.292 versus AI TTR of 0.558 to 0.694. The paper also says humans are higher on all three features, contradicting its figures, values, and later examples. | Direct figures, qualitative examples, and an internal prose inconsistency. Human examples are 822 to 1,191 tokens and AI examples 108 to 190, so length strongly affects TTR. The figures support higher AI TTR, while absolute hapax counts are higher for the longer human texts. | Challenges current behaviour: `check_type_token_ratio` in `human-eyes/scripts/grade.py` flags only ratios at or below 0.40 after 150 words. `patterns.json` and `patterns.md` state that AI prose often has lower diversity and human prose reaches further into the lexicon. The paper's reported TTR and token counts put all three human passages on the live threshold's flagged side and all three AI passages on its clear side. This is a threshold comparison, not an executed `grade.py` result: the figure text is truncated and the paper and checker use different tokenisation. | The current one-way threshold is not defensible from this source without length- and register-matched validation. The source itself cannot resolve the true direction because its prose is inconsistent and its selected pairs are sharply length-mismatched. | If approved, treat pattern #53 as a release-significant reevaluation: test direction, length sensitivity, lemmatization, lexical density, hapax rate rather than raw count, and genre controls on the real paired corpora. Until the user decides, make no change to `grade.py`, `patterns.json`, `patterns.md`, guidance, or tests. | approved | not started |
| C07: Lexical-only classifiers outperform the full 284-feature set by 14.29 F1 points in cumulative TB7 ablation and help many TB8 pairs, but underperform the full feature set on nearly every matched in-domain pair. | Direct cumulative and lexical-only ablations. The apparent benefit reverses with evaluation condition, showing that extra features can be useful in-domain and noisy out-of-domain. | Partly covered and qualifying: H12 and H25 anticipate condition dependence, while `vocabulary-diversity` remains one fixed document check. The `overall-signal-stacking` meta-check uses fixed weights for catalogue components and excess-vocabulary evidence, but it does not include TTR or reproduce the paper's SVM feature inventory. | No current release gate tests whether a statistical check adds signal or noise by register, model family, and transfer condition before it can contribute to findings. | If approved, add an ablation requirement to any future statistical-check release gate: compare the full catalogue with the candidate feature removed across matched human controls and held-out conditions. Do not change aggregate weights from this paper alone. | pending | not started |
| C08: SQuAD has almost no lexical-richness dependence, while DialogSum is poorly detected and improves when morphology and information features are removed; other non-lexical areas vary or add noise by domain and model. | Direct domain-specific ablation and counterexamples. Some domain-category explanations are author interpretations, and DialogSum is one unseen conversational dataset generated by GPT-4. | Partly covered: H12 and `genre_specific` require genre-sensitive interpretation; `STRATEGY.md` requires treatment in context. There are no live morphology, psycholinguistic, information-theoretic, or emotion-density checks that duplicate these feature areas. | Broad genre labels do not capture factual QA, conversational summary, source length, or model interaction. The paper argues against a universal fixed feature inventory. | Record these as counterexamples. If approved, require every statistical-feature proposal to include at least one short-answer or factual-QA control and one conversational control. Do not promote new morphology, emotion, or information checks from these results. | pending | not started |
| C09: Displayed Yelp examples show human reviews with more anger, disgust, and low-valence tokens, while paired AI continuations are more positive. The authors selected emotion as a feature area and Yelp plus text-davinci-003 as a domain-model pair to make the experimental contrast clear. | Direct qualitative examples using NRC lexicons. The source states how the feature area and domain-model pair were chosen, but not how the three individual displayed pairs were sampled. It does not establish a universal emotion direction across genres or models, and the illustration is not a prevalence estimate. | Not covered as an emotion-frequency metric: `tonal_uniformity` and `neutrality_collapse` assess register and stance, not emotion-lexicon counts. No live check claims anger or positivity frequency as an AI tell. | Treating the displayed examples as generic positivity evidence would exceed their sampling and domain scope. | Record only as qualitative illustration. Do not add sentiment, valence, anger, or disgust checks. A future proposal would need direct distribution tables, matched lengths, multiple domains, human controls, and false-positive review. | pending | not started |
| C10: In the ChangeMyView pilot, restricting both sides to shared OpenAI and LLaMA families raises reverse cross-dataset Macro F1 from 0.435 to 0.850; the authors infer model alignment matters more than prompt wording. | Direct small cross-dataset pilot on one domain. Prompt and dataset changes are not fully factorial, so the relative prompt-effect conclusion is qualified and may retain confounding. | Partly covered: H25 tracks model-family residue; `dev/TESTING.md` records prompt and generation provenance; the pending Xia recommendation already proposes a factorial prompt-model-domain lane. | The project cannot estimate prompt versus model effects from its current corpus, and this pilot does not isolate them sufficiently for a product rule. | If approved, add the pilot as supporting evidence for the already pending factorial robustness lane, with an explicit requirement not to infer prompt causality from non-factorial cross-dataset comparisons. No checker change. | pending | not started |
| C11: The study is limited to English, a finite and partly dated model set, one feature toolkit, and linear SVMs; main experiments do not vary prompts, and the aggregate results do not establish an individual-document authorship rule. | Direct method and limitation statements, plus a reviewer boundary on what aggregate classifier results can establish. The paper identifies limits involving language, newer models, and prompt variation rather than demonstrating generality beyond those conditions. | Fully covered in principle: `STRATEGY.md` rejects authorship classification; `dev/TESTING.md` requires provenance and varied genres/registers; source metadata records model, language, and corpus scope. | This source's exact limitations were not previously in the evidence library. | Record only in this card and source indexes. Do not change product behaviour or generalise beyond the reviewed conditions. | pending | not started |
| C12: The paper discloses GitHub Copilot for code completion and Grammarly for spelling and grammar correction, and separately discusses dual-use risks of interpretable detection research. | Direct AI-assistance and ethics sections. Disclosure identifies tools and dates but does not quantify affected text or code, and it is not evidence of invalid results or AI authorship. | Not covered: source cards can record provenance, but there is no standard project field for disclosed code, editing, grammar, or translation assistance. The academic genre watchlist checks citations and evidence rather than assistance disclosure. | A general provenance convention could improve source review, but treating disclosed assistance as a prose finding would violate the project's non-authorship boundary. | If approved, add an optional source-card provenance field for disclosed assistance type, tool, date, and stated role. Keep it informational and never convert disclosure into a pattern flag or credibility penalty. | pending | not started |
| C13: Related-work statements about lexical diversity, syntactic regularity, morphosyntactic and semantic indicators, and other prior stylometric cues are claims inherited from cited studies rather than universal direct findings of this paper. | Indirect evidence. This paper measures its own feature set and ablations, but its descriptions of what earlier studies found remain source-attributed background until those upstream works are directly reviewed for the proposed use. | Fully covered as a non-promotion requirement: the source-ingest contract and `human-eyes/references/sources/README.md` require direct upstream review before a cited claim supports a project recommendation. | The cited studies do not all have claim-specific direct reviews tied to this card. | Record only. Do not promote inherited feature-direction claims from this card. If a future recommendation depends on one, ingest and independently review the exact upstream source first. | pending | not started |

## Recommendations

- C01: If approved, extend the pending robustness-lane proposal with a small predeclared feature-area ablation matrix and complete condition metadata.
- C02: Record condition-specific transfer failures and require weak-case plus uncertainty reporting in that lane; do not import classifier scores as product thresholds.
- C03: If approved, extend H25 work to preserve direction-specific model-family transfer and model-version metadata, and preserve correlation as co-variation rather than distance evidence; no live checker change.
- C04: If approved, make domain and register mandatory grouping fields in the robustness lane; do not map MAGE dataset names directly to live thresholds.
- C05: If approved, reevaluate pattern #53 with length- and register-matched controls plus multiple lexical-richness measures before adding any new metric.
- C06: If approved, run a release-significant review of the direction and threshold of `vocabulary-diversity`; keep all product files unchanged while the decision is pending.
- C07: If approved, add feature-ablation evidence to future statistical-check release gates; do not change aggregate weights from this paper alone.
- C08: Preserve SQuAD and DialogSum as counterexamples and require short factual plus conversational controls for future statistical features; add no new checks.
- C09: Record the selected emotion examples only; do not promote sentiment or emotion-frequency checks without broader direct evidence.
- C10: Add the qualified pilot as support for the already pending factorial robustness lane; do not infer prompt causality from this comparison.
- C11: Record the language, model, prompt, toolkit, and classifier limits in this source record only; no product change.
- C12: If approved, add an optional informational source-provenance field for disclosed assistance; never turn disclosure into a prose flag or credibility penalty.
- C13: Record-only non-promotion decision; an upstream feature-direction claim requires a separate, independently reviewed source record before use.

## Evaluation of approved changes

- C01: not applicable - recommendation pending; no product change implemented.
- C02: not applicable - recommendation pending; no product change implemented.
- C03: not applicable - recommendation pending; no product change implemented.
- C04: not applicable - recommendation pending; no product change implemented.
- C05: not applicable - recommendation pending; no product change implemented.
- C06: not applicable - recommendation pending; no product change implemented.
- C07: not applicable - recommendation pending; no product change implemented.
- C08: not applicable - recommendation pending; no product change implemented.
- C09: not applicable - record-only recommendation pending; no product change implemented.
- C10: not applicable - recommendation pending; no product change implemented.
- C11: not applicable - record-only recommendation pending; no product change implemented.
- C12: not applicable - recommendation pending; no product change implemented.
- C13: not applicable - non-promotion recommendation pending; no product change implemented.

## Document review

- **Review status:** passed
- **Review method:** independent source-record reviewer: Codex CLI fresh process, did not perform the extraction
- **Findings resolved:** Qualified the classifier's hapax normalisation as an inference from the preprocessing rule; changed the XSum TTR comparison from an implied checker execution to an explicit threshold-only comparison with tokenisation and truncation limits; distinguished selection of the emotion feature area and Yelp domain-model pair from unknown sampling of the three displayed examples.
- **Unresolved findings:** none
