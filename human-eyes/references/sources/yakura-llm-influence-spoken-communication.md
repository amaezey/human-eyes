# Yakura et al.: Empirical Evidence of Large Language Model's Influence on Human Spoken Communication

## Metadata

- **URL:** https://arxiv.org/abs/2409.01754v4
- **Author / owner:** Hiromu Yakura, Ezequiel Lopez-Lopez, Levin Brinkmann, Ignacio de la Serna, Lara Kirfel, Prateek Gupta, Ivan Soraperra, Thomas F. Eisenmann, Dirk U. Wulff, and Iyad Rahwan (Max Planck Institute for Human Development, TUD Dresden University of Technology, and Vienna University of Economics and Business)
- **Published:** v1 submitted 2024-09-03; the reviewed v4 submitted 2026-07-16
- **Retrieved:** 2026-07-26
- **Extracted:** 2026-07-26
- **Source type:** academic preprint (arXiv, not a journal version of record); combines a quasi-experimental corpus study with a preregistered randomised controlled experiment
- **Evidence tier:** Peer-reviewed / academic empirical (academic preprint; strongest tier available for pattern evidence, still aggregate and register-scoped)
- **Review mode:** new
- **Stable identifier:** arXiv:2409.01754v4. No `arxiv:doi` and no `journal_ref` in the arXiv Atom API record; no publisher DOI found. The paper cites preregistration AsPredicted #k38u44.
- **Version / revision:** arXiv v4, 65 pages. v3 (submitted 2025-07-08) was compared at the abstract and submission-history level only; no v3 full text was retrieved.
- **Full-text status:** complete
- **Snapshot:** `snapshots/yakura-llm-influence-spoken-communication.md`
- **Extraction method:** PDF-to-text. User-supplied local PDF verified against the arXiv Atom API record for `2409.01754v4`; complete embedded text layer of all 65 pages extracted with Poppler `pdftotext -layout`; structure checked with `pdfinfo` and `pdfimages -list`; pages 4 and 7 rendered with PyMuPDF and read visually to recover figure word labels that are drawn as vector paths.
- **Snapshot SHA-256:** `e6f82b0c9904e4858485452219872ad06cc8a81df03883a700d250c7d76e53cb`
- **Model / corpus scope:** GPT scores computed from GPT-3.5-turbo (`gpt-3.5-turbo-1106`), GPT-4 (`gpt-4-0613`), and GPT-4-turbo (`gpt-4-turbo-2024-04-09`), with GPT-4o (`gpt-4o-2024-05-13`) and GPT-5 (`gpt-5-2025-08-07`) evaluated for comparison but excluded from the reference score. Score corpora are human-written texts and their GPT-edited counterparts: arXiv abstracts 2019-2022 (7,182), bioRxiv abstracts 2019-2022 (2,880), Nature abstracts 2019-2023 (over 8,000), Enron emails 2000-2001 (10,000), Hewlett Foundation student essays 2012 (10,000), Wikipedia articles 2019-2022 (10,000), and 2,000 pre-ChatGPT spontaneous podcast excerpts. Outcome corpus is English-language podcast episodes published 2017-01-01 to 2026-04-30: 1,407,131 candidates, 931,450 transcribed, 824,634 passing the spontaneity screen, 737,083 hours. Appendix B replicates on 360,445 academic YouTube talks from 20,622 institutions. The experiment ran on Prolific 2026-04-16 to 2026-04-20, N = 496, chatbot `gpt-4o`.
- **Access limitations:** The word labels inside Figure 1D, Figure 3A, and Figure 3B are vector paths and are absent from the text layer; they were read visually from high-zoom renders and are recorded in the snapshot as reviewer-read transcriptions rather than extracted text. Figures S14 to S16 extract their axis labels through a broken font-encoding map (a uniform one-character-forward shift); the same 18 synonym pairs are printed correctly in Table S7, so no content is lost, and the shifted letters are preserved verbatim. The same broken subset maps punctuation and digits 31 code points below their intended glyph, so `space`, `-`, `.`, `/`, `0`, `1`, `2`, `5`, and `7` extract as control bytes (488 of them) and left the raw extraction unreadable to standard text tooling. Each is restored to the glyph it stands for rather than deleted, so no glyph is lost. The one byte dropped is NUL, 2,337 occurrences of inter-character padding that decodes to another control code and stands for no glyph. Poppler's per-page form feeds are replaced with explicit `===== PDF page N =====` markers so the 65 page boundaries stay countable. Plotted data values are graphical and are recoverable only where the paper prints them in body text, captions, or tables. The supplementary front matter declares Figures S1 to S17 while the document contains Figures S1 to S20. The v4-versus-v3 comparison in C29 is abstract-level only: the v3 abstract and the arXiv submission history are preserved in the snapshot under `Prior version compared`, but no v3 full text was retrieved, so any v3 detail beyond its abstract is outside this record. The AsPredicted preregistration, the PodcastIndex catalogue, the release materials holding the annotation manual and classifier metrics, and all 96 cited works were not retrieved.

## Summary

A Max Planck Institute for Human Development preprint measuring whether words ChatGPT prefers when editing human text later increase in unscripted human speech. The word list itself is produced from written prose: a per-word GPT score is the log-odds ratio of document frequency between human-written texts and their GPT-edited counterparts, aggregated across seven corpora, three rephrasing prompts, and three pre-GPT-4o chat models. Those scores are then tested against 737,083 hours of English podcast conversation from 824,634 spontaneity-screened episodes spanning 2017 to April 2026, using synthetic-control matching plus Bayesian change-point regression anchored to the 2022-11-30 ChatGPT release, with in-space and in-time placebo tests. A preregistered experiment (N = 496) supplies the individual-level mechanism. For human-eyes the paper contributes two things. First, a complete, ranked, verbatim word list: the 20 highest-scoring GPT-preferred words, the 36 words in the top 1% of GPT scores, and a per-word post-release slope for each, which is directly actionable against `AI_VOCABULARY` in `grade.py`. Second, a dated measurement that the same vocabulary is entering spontaneous human production, plus a reversion for some words after mid-2024, both of which bear on how long any word-list check keeps separating generated from ordinary prose. The corpus is English-only, drawn from a self-selected public-facing podcast population, and the causal window covers the first 18 months after release.

## Main insights

- The word list's provenance is written prose, not speech. The GPT score compares human-written texts against the same texts after a model was asked to polish, improve, or clarify them. Speech is the outcome the paper tests, not the source of the list.
- The 20 top GPT-preferred words by combined reference score, in the paper's own rank order: `delve`, `underscored`, `comprehend`, `bolster`, `boasts`, `inquiry`, `swift`, `meticulous`, `pinpoint`, `groundbreaking`, `surpassed`, `swiftly`, `craft`, `intricate`, `heightened`, `lessen`, `scrutinized`, `discerning`, `necessitated`, `showcase`.
- Appendix A ranks the same set numerically: `delve` alone above log-odds ratio 4, then `underscore`, `comprehend`, `bolster`, `boast`, `inquiry`, `swift`, `meticulous`, `pinpoint`, and `groundbreak` above 2.5.
- Figure 3A prints the twelve largest credible changes as `delve`, `showcase`, `groundbreaking`, `innovative`, `boast`, `intricacies`, `meticulous`, `surpassed`, `alongside`, `hinges`, `crucial`, and `underscore`. Table S2 and Figure S2 name all 36 words in the top 1% of GPT scores with their per-word post-release slope. Twenty-eight of the 36 rose after the release, 13 credibly so; eight fell, two credibly.
- The effect is graded by score, not vocabulary-wide: mean post-release slope is +0.030 in the top 1%, +0.025 in the top 2%, +0.015 in the top 5%, +0.013 in the top decile, and +0.004 across the whole 3,535-word vocabulary.
- `delve` in Science and Technology podcasts ran about 44% above its synthetic control over months 13 to 18 after release, with a placebo p of 0.010; Education +32%, Business +31%, category-agnostic +9%, Sports -7%.
- The rise did not hold. The gap peaked around mid-2024 and then fell below baseline: `delve` at -15% in Science and Technology, -11% in Education, -30% in Business, and -35% in the category-agnostic sample. The turn coincides with the free release of GPT-4o and with public awareness of `delve` as an LLM signature.
- `boast`, `meticulous`, and `showcase` kept rising while `delve` and others reverted.
- The generator itself moved. The `delve` odds ratio in revised arXiv abstracts falls from roughly 380:1 under GPT-3.5-turbo to 100:1 for GPT-4-turbo and 40:1 for GPT-4o; under GPT-5 most signature words are no longer overused and `delve` sits at roughly 0.7:1, below its human baseline.
- The effect is one-directional. Words ChatGPT disfavours show no matching decline in speech: bottom 1% mean slope -0.002 against a chance range of -0.007 to +0.014.
- Word preferences are robust to the rephrasing instruction. Polish, improve, and clarify give closely similar log-odds ratios for every reference word, with clarify mildest and never reversing a preference.
- Log-odds magnitudes vary substantially by source corpus, and the paper attributes that variation primarily to baseline differences in human word choice. `underscore` is its worked example: humans rarely use it in essays, and GPT revisions introduce it frequently across domains.
- Raw stem counts are contaminated. `swift` sweeps in Taylor Swift, Apple's Swift language, the SWIFT banking network, and NASA's Swift telescope. The paper runs a word-sense audit that keeps only curated in-scope senses and reports that it removes a substantial share of raw matches for contaminated words.
- Among the largest movers overall, `delve` sits at GPT-score rank 1.00 and the three next-highest-ranked risers are `prompted` (0.98), `invaluable` (0.94), and `captive` (0.88).
- Ranking words by how much they moved, without conditioning on GPT preference, recovers a different and mostly irrelevant set: the steepest falls are `pandemic`, `vaccine`, `corona`, and `virus`, and several of the steepest rises are ordinary technical vocabulary (`gpu`, `python`, `vector`, `queries`, `transcript`) whose GPT scores sit mid-distribution.
- The eleven stems excluded as prompt artefacts, because models repeat them in their own responses, are `rephrase`, `polish`, `dear`, `text`, `certainly`, `subject`, `readable`, `clarity`, `enhance`, `version`, and `title`.
- In the preregistered experiment, participants used the chatbot's assigned synonym variant 61% of the time against 17% for the alternative during interaction, 58% against 23% when describing entirely new images after a three-minute distractor, and chose it on 63% of forced-choice trials. The effect held across nouns, verbs, and adjectives.
- Only 15 of 496 participants (3.0%) noticed the chatbot's vocabulary pattern, so the uptake was implicit rather than deliberate.
- The 18 experimental synonym pairs are ordinary counterbalanced word pairs, not AI-preferred vocabulary. Which member was the AI variant was assigned at random, so both halves served as the AI variant across conditions.
- The paper's specificity argument: the top-1% words have no referential connection to language models and sit in a frequency band where natural lexical change is slow, unlike the COVID-19 and machine-learning vocabulary its own corpus also shows moving.
- The reviewed v4 supersedes the v3 figures the library currently cites second-hand. v3's abstract reported 740,249 hours across 360,445 YouTube academic talks and 771,591 podcast episodes and named `delve, comprehend, boast, swift, and meticulous`; v4 reports 737,083 hours from 824,634 spontaneity-screened podcast episodes, demotes the YouTube corpus to an appendix replication, and names `delve, showcase, boast, intricacies and meticulous`.

## Evidence and claims to extract

- **Direct source reviewed:** arXiv:2409.01754v4, the complete 65-page PDF including Methods, Appendix A, Appendix B, the reference list, and the Supplementary Materials with Figures S1 to S20 and Tables S1 to S7. The vector-drawn word labels in Figures 1D, 3A, and 3B were read visually from high-zoom renders of the same PDF.
- **Method and sample:** Two studies. (1) Observational. Per-word GPT score from log-odds ratios of document frequency between human-written texts and their GPT-edited counterparts, Bayesian-marginalised over dataset, model, and prompt. Outcome measured over English podcast episodes 2017-01-01 to 2026-04-30, screened by duration (15 minutes to 5.5 hours), English metadata, speaker diarisation (two or more speakers, four or more alternating turns in a middle 10-minute slice), and a Whisper-large-v3-encoder spontaneity classifier trained on 399 human-annotated episodes (interrater ICC 0.87) at a retention threshold of 3.0 on a 1-to-4 scale, about 90% accuracy. Transcription by WhisperX large-v3, held fixed across the whole time window. Text preprocessed by tokenisation, lowercasing, NLTK stop-word removal, non-alphabetic filtering, a three-character minimum, and Porter stemming, with stems relabelled to a representative surface form. Synthetic control per treated word from a 100-word donor pool, excluding the 20 nearest word2vec neighbours and restricted to the central 50% by absolute GPT score; weights by SLSQP on Gaussian-process-smoothed pre-treatment trajectories; effect estimated as the post-release slope of a Bayesian change-point regression on the observed-minus-synthetic log10 gap; significance by in-space placebo (n = 100) and an in-time change-point date sweep (n = 28 pre-GPT candidates). Four robustness specifications C1 to C4 vary donor selection, pool size, weighting, and the audited-versus-raw counts substrate. 3,535 stems analysed. (2) Experimental. Preregistered 2 group by 2 mode by 12 trial design, N = 496 after four exclusions, recruited on Prolific 2026-04-16 to 2026-04-20; 18 synonym pairs across nouns, verbs, and adjectives; `gpt-4o` chatbot covertly prompted to a canonical variant; outcomes scored by stemmed-span matching on human-verified transcripts; Monte Carlo permutation tests and cluster bootstrap.
- **Direct versus cited evidence:** C01 to C28 and C30 are measured or reported directly by this paper. C29 is a provenance finding established by comparing the reviewed v4 against the v3 abstract and arXiv submission history, not a claim the paper makes. The paper's statement that `delve` already rose in scientific abstracts and peer reviews (part of C27) is inherited from Liang et al. and is marked as such; that paper is not ingested here. The Geng et al. academic-conference-talks corroboration named alongside Appendix B is likewise cited, not measured here.
- **Important limits and counterexamples:** The corpus is English-only and drawn from a self-selected, public-facing population of podcast hosts and guests. The main causal window is the first 18 months after release and treats OpenAI's models as the dominant driver; the paper notes that deployment has since fragmented. The stable-unit-treatment-value assumption is named as the binding one and cannot be guaranteed, because ChatGPT may shift all words rather than only the ones it prefers; the authors argue the differential effect survives residual interference. The experiment used self-identified native English speakers on Prolific in a single session after a brief distractor, so it captures only an early signal. Two directional counterexamples matter for any word-list use: several top-1% words fell rather than rose, and `delve` itself reverted below baseline after mid-2024 while the generator stopped preferring it. A third is the unconditioned view, which shows that large credible movement by itself identifies pandemics and product cycles rather than model preference.

## Skill-use audit

- **Good use:** A ranked, dated, verbatim candidate list for #7 `no-ai-vocabulary-clustering`, measured on written human prose against its model-edited counterpart, which is the register human-eyes reads. Also a documented per-model attenuation curve for the same words, and a documented reversion in human use, both of which are exactly the kind of dated evidence H24 and H25 ask for.
- **Misuse / overclaim:** Treating any single word as evidence about a document. The paper's own unit is the monthly relative frequency of a word across hundreds of thousands of episodes, and its own top-1% set contains words that fell.
- **Unsupported use:** Any inference about who wrote a text, any spoken-register rule (human-eyes reads text and does not process audio), any claim about non-English writing, and any use of the 18 experimental synonym pairs as AI vocabulary, since which half was the AI variant was randomly assigned.
- **Underused evidence:** The word-sense contamination result is the practical part the project has never had from a vocabulary source: it names, with examples, which of its own candidate words carry brand, proper-noun, and unrelated-sense traffic that a substring matcher would sweep in.
- **Patterns left on the table:** The 27 named words the live matcher does not recognise; the five prompt-artefact stems C19 proposes and the five it cannot bound; `prompted` and `captive` from the preference-blind panel; the measured null on ChatGPT-disfavoured words; the per-model attenuation curve as a dated stamp for existing #7 entries.

## Matched patterns / rules

- Programmatic `no-ai-vocabulary-clustering` (#7) and its two lists in `human-eyes/scripts/grade.py`, `AI_VOCABULARY` (82 entries) and `AI_VOCABULARY_REGEX`, plus the frozen 100-row `GPTZERO_AI_PHRASES` tuple that `_find_ai_words` searches alongside them. Severity `strong_warning` in `human-eyes/scripts/patterns.json`; the check fails when one paragraph carries three or more matches, or when two distinct Kousha-Thelwall term families appear anywhere in the document.
- `KOUSHA_THELWALL_TERM_FAMILY_REGEX` in `grade.py`, whose twelve families include `underscore`, `delve`, `showcase`, `intricate`, `meticulous`, `heighten`, and `bolster`, all of which this paper independently ranks in its top 1%.
- Programmatic `no-collaborative-artifacts`, which already flags the `Certainly!` opener that this paper had to exclude as a prompt artefact.
- Existing source cards this paper is upstream of or corroborates: `kobak-llm-excess-vocabulary.md`, `juzek-ward-delve.md`, `kousha-thelwall-academic-papers.md`, `geng-trotta-transforming-academic-style.md`, `gptzero-ai-vocabulary.md`, `grammarly-common-ai-words.md`, `graham-delve-post.md`.
- Cards that currently cite this work second-hand, at the differing depths recorded in C29: `fairbanks-atlantic-ai-writing.md` (C17), `slate-ai-shaming-paranoia.md` (C09), `kriss-nyt-ai-write-like-that.md` (C21), `chitalian-offensive-ai-speak.md` (C17), and the citation preserved in the `wikipedia-signs-of-ai-writing.md` and `abdulhai-llms-distort-written-language.md` snapshots.

## Associated hypotheses

- H24 register-specific vocabulary density: this is the first ingested source that supplies a per-word score, a per-word direction, a per-model attenuation curve, and a dated reversal for the same word set, which is what H24's test asks for.
- H25 model-family versus generic-AI residue: the paper measures the same words across five GPT builds and shows the preference attenuating to nothing by GPT-5, which is a direct model-version drift result for words already live in #7.
- H12 genre-aware threshold calibration: log-odds magnitudes vary substantially across abstracts, emails, essays, Wikipedia, and podcast transcripts, and the paper attributes that variation to human baseline differences by register.
- No new hypothesis is proposed.

## Questions / follow-up

- Whether the reverted words, `delve` above all, keep their current #7 membership unchanged, carry a dated note, or are re-measured. The project corpora cannot answer this on their own: `delve` occurs zero times in both sample sets.
- Whether Liang et al. 2024, the source of the written-footprint claim inside C27 and the origin of the GPT-score method, should receive its own card. It is currently indirect.
- Whether the Geng et al. academic-conference-talks paper named as concurrent corroboration for Appendix B should be ingested; the library already holds two other Geng and Trotta cards.
- The AsPredicted preregistration, the released annotation manual, and the classifier metrics were not retrieved and would be needed to check the experiment's analysis against its plan.

## Update provenance

Not applicable: initial ingestion.

## Decision history

- 2026-07-26 (DR-165A): Mae ruled DR-165's inflection bug fixed completely. Every Yakura word approved under C01 and C03 was approved on a corpus count taken over its inflection family, but most shipped as a single surface form, so 43 inflected forms across 20 families matched nothing and the substring list leaked into host words such as `Inquiryless`. All of them now sit in `AI_VOCABULARY_REGEX` as word-bounded inflection families. That closes C10, because `boast` and its inflections are now covered where only `boasts` was. Mae separately approved `clarity`, `polish` and `readable` from C19 on the project-corpus measurement. `certainly` and `rephrase` from C19, and C17's `prompted` and `captive`, remain unruled under DR-28. #7 moves from 11% of human documents and 5% of generated to 11% and 8%.
- 2026-07-26 (DR-165B): Mae rejected the four remaining vocabulary candidates, C17's `prompted` and `captive` and C19's `certainly` and `rephrase`. Every word on both Yakura lists is now ruled. No checker change.
- None: initial review. Recorded for context rather than as a decision: this is the first direct ingestion of a work the library has cited second-hand since 2026-07-14. The prior indirect records quote the v3 abstract, whose corpus figures and headline word list the reviewed v4 supersedes; C29 carries that as its own claim so the affected cards can be corrected under a ruling rather than silently.

## Project coverage

This is the authoritative review table. Give every relevant source claim or example a stable claim ID in the first cell.

Escape any literal pipe within a table cell as `\|`; an unescaped extra pipe makes the row invalid.

| Source claim or example | Evidence assessment | Existing project coverage | Missing or challenged | Recommendation | User decision | Implementation status |
|---|---|---|---|---|---|---|
| C01: The 20 top GPT-preferred words by combined reference GPT score, in the paper's printed rank order (Figure 1D): `delve`, `underscored`, `comprehend`, `bolster`, `boasts`, `inquiry`, `swift`, `meticulous`, `pinpoint`, `groundbreaking`, `surpassed`, `swiftly`, `craft`, `intricate`, `heightened`, `lessen`, `scrutinized`, `discerning`, `necessitated`, `showcase`. | Direct measurement. Combined reference score over GPT-3.5-turbo, GPT-4, and GPT-4-turbo on scientific abstracts, computed as a Bayesian-marginalised log-odds ratio of document frequency between human-written text and its model-edited counterpart. Written-prose provenance, which matches the register human-eyes reads. Word labels read visually from the vector figure; the same twenty appear as text in the Appendix A ranking sentence and Figures S11 and S12. | Ran `grade._find_ai_words` on each word against the live registry on 2026-07-26. Eight are already #7 clustering candidates: `delve`, `underscored`, `bolster`, `meticulous`, `groundbreaking`, `intricate`, `heightened`, `showcase`. **Partly covered.** | Twelve of the twenty produce no match: `comprehend`, `boasts`, `inquiry`, `swift`, `pinpoint`, `surpassed`, `swiftly`, `craft`, `lessen`, `scrutinized`, `discerning`, `necessitated`. Note that `surpass` matches but the paper's inflected `surpassed` does not, because `AI_VOCABULARY_REGEX` word-bounds it; `comprehensive` is present but does not reach `comprehend`. Measured on the project corpora 2026-07-26 (55 human documents / 137,185 words; 38 generated / 45,638 words, counting `[A-Za-z']+` tokens). Families are inflection-only, not derivational: `craft` is `craft(?:s\|ed\|ing)?` and excludes `craftsman`, which would give 21/11 instead; `discerning` is measured as `discern\w*` and returns 0 as a bare surface form. On that basis the twelve run: `craft` 17 occurrences in 9 human documents against 6 in 3 generated; `comprehend` 3/2 against 0/0; `discerning` 2/2 against 1/1; `inquiry` 1/1 against 1/1; `boast` 4/2 against 0/0; `pinpoint` 1/1 against 0/0; and `swift`, `swiftly`, `surpass`, `lessen`, `scrutinize`, and `necessitate` at 0/0 in both sets. Six of the twelve are therefore silent in this corpus rather than measured against it, which is a scope fact about the samples and not a result about the words. | Programmatic. Add the twelve uncovered words to #7's vocabulary in `grade.py` as clustering candidates so they never fail alone and only count toward the existing three-per-paragraph condition. Severity in `patterns.json` unchanged. Route the polysemous members through `AI_VOCABULARY_REGEX` with word boundaries rather than `AI_VOCABULARY`, whose entries are substring-matched, and handle `swift` under C15's separate treatment because word boundaries alone do not clear its brand and acronym collisions. Verify with `dev/evals/tests/test_grade.py` and the full test loop. | approved with changes | implemented |
| C02: Appendix A ranks the same set numerically: `delve` alone above log-odds ratio 4, followed by `underscore`, `comprehend`, `bolster`, `boast`, `inquiry`, `swift`, `meticulous`, `pinpoint` and `groundbreak` above 2.5. | Direct measurement, stated as body text rather than read from a figure, so it independently confirms the C01 transcription for the top ten. Scope is scientific abstracts (arXiv, bioRxiv, Nature) and the three pre-GPT-4o chat models. | Same #7 mechanism as C01. `delve`, `underscore`, `bolster`, `meticulous`, `groundbreaking` match; `comprehend`, `boast`, `inquiry`, `swift`, `pinpoint` do not. **Partly covered.** | Nothing beyond C01; this row exists to record that the top ten are attested in extracted text and not only in a reviewer-read figure. | Record only. Use C02 as the text-attested corroboration for the C01 transcription; the additions themselves are ruled under C01. No separate product change. | pending | not started |
| C03: The 36 words in the top 1% of GPT scores, with their post-release change-point slope (Table S2 ordering, slopes from Figure S2): `delve` +0.12, `showcase` +0.08, `groundbreaking` +0.09, `innovative` +0.04, `boasts` +0.11, `meticulous` +0.08, `underscored` -0.06, `intricacies` +0.09, `surpassed` +0.07, `alongside` +0.04, `hinges` +0.07, `crucial` +0.04, `creation` +0.03, `bolster` +0.08, `notice` -0.01, `intricate` +0.02, `craft` +0.03, `pinpoint` -0.02, `comprehend` +0.04, `swift` -0.02, `inquiry` +0.00, `lessen` +0.02, `groundwork` -0.01, `heightened` +0.02, `escalating` -0.02, `discerning` +0.04, `inaugural` +0.02, `affirmed` +0.00, `notably` +0.03, `portrayed` -0.02, `catering` +0.02, `reliant` -0.00, `impressive` +0.01, `thorough` +0.02, `akin` -0.00, `spotlight` +0.02. | Direct measurement. Table S2 prints all 36 as text with per-word pre-treatment fit statistics; Figure S2 prints the same 36 with slopes in panel titles. This is the complete treated set for the headline analysis, so nothing is selected after the fact. | Checked all 36 against the live matcher on 2026-07-26. Twelve match: `delve`, `showcase`, `groundbreaking`, `innovative`, `meticulous`, `underscored`, `intricacies`, `crucial`, `bolster`, `intricate`, `heightened`, `notably`. **Partly covered.** | Twenty-four produce no match: `boasts`, `surpassed`, `alongside`, `hinges`, `creation`, `notice`, `craft`, `pinpoint`, `comprehend`, `swift`, `inquiry`, `lessen`, `groundwork`, `escalating`, `discerning`, `inaugural`, `affirmed`, `portrayed`, `catering`, `reliant`, `impressive`, `thorough`, `akin`, `spotlight`. Combined with C01 the union across both lists is 39 words, of which 12 are covered and 27 are not. Measured on the project corpora 2026-07-26 with the same inflection-only families and `[A-Za-z']+` token counts as C01 (human occurrences / documents against generated occurrences / documents): `notice` 26/17 against 26/18, `craft` 17/9 against 6/3, `alongside` 7/3 against 3/3, `creation` 7/5 against 2/2, `boast` 4/2 against 0/0, `comprehend` 3/2 against 0/0, `thorough` 2/2 against 2/2, `escalate` 2/1 against 1/1, `discerning` 2/2 against 1/1, `impressive` 1/1 against 5/5, `hinge` 1/1 against 2/2, `pinpoint` 1/1 against 0/0, `inquiry` 1/1 against 1/1, `affirm` 1/1 against 0/0, `portray` 1/1 against 1/1, `groundwork` 0/0 against 1/1, `catering` 0/0 against 1/1, and eleven silent in both sets (`swift`, `swiftly`, `surpass`, `lessen`, `scrutinize`, `necessitate`, `inaugural`, `reliant`, `akin`, `spotlight`, plus already-covered `delve`). | Programmatic. Put the full 36 to Mae as one #7 vocabulary decision alongside C01, distinguishing the 28 whose slope is positive from the eight that fell, and separating the high-traffic ordinary words from the register-marked ones, because the first group will contribute matches in ordinary prose and the second will not. The corpus measurement identifies that first group concretely: `notice` appears in 31% of human documents, and `craft`, `alongside`, and `creation` each in at least three. Same clustering-candidate mechanism, same severity, same failure condition. Verify with `dev/evals/tests/test_grade.py` and the full test loop. | approved with changes | implemented |
| C04: Twelve words showed the largest credible post-release change, printed in Figure 3A in this grid order: `delve` +0.12, `showcase` +0.08, `groundbreaking` +0.09, `innovative` +0.04; `boast` +0.11, `intricacies` +0.09, `meticulous` +0.08, `surpassed` +0.07; `alongside` +0.04, `hinges` +0.07, `crucial` +0.04, `underscore` -0.06. Eleven rise; `underscore` is the lone decline. | Direct measurement; the twelve are the largest-magnitude subset by the conservative slope bound (the 95% HDI limit nearest zero), so they are the paper's own strongest cases rather than a reviewer selection. Fifteen of the 36 showed a credible change; these are the twelve largest. Table S2's caption says its rows carry the Figure 3A order, and its first twelve rows carry the same twelve words in a different order; the word set and all twelve slopes agree, and the ordering inconsistency is the paper's own. | Eight of the twelve already match #7: `delve`, `showcase`, `groundbreaking`, `innovative`, `intricacies`, `meticulous`, `underscore`, and `crucial`. **Partly covered.** | Four do not match: `boast`, `alongside`, `hinges`, and `surpassed`. `underscore` is live in #7 and is the one word here whose spoken use fell rather than rose. | Programmatic. If Mae prefers a narrower addition than C01 or C03, this twelve-word set is the defensible minimum: it is the paper's own credible-change subset, and it needs only four additions. Same #7 clustering-candidate mechanism and severity. Record `underscore`'s negative direction with it rather than dropping the word. | pending | not started |
| C05: The 19 words with the highest average log-odds ratio across all datasets, models, and prompts (Figures S11 and S12): `delve`, `underscored`, `comprehend`, `bolster`, `boasts`, `inquiry`, `swift`, `meticulous`, `pinpoint`, `groundbreaking`, `surpassed`, `swiftly`, `craft`, `intricate`, `heightened`, `lessen`, `scrutinized`, `discerning`, `necessitated`. | Direct measurement, stratified by dataset and model with standard errors over the three prompts. This is the Figure 1D top 20 minus `showcase`, extracted as page text rather than read from a figure, so it independently corroborates 19 of the 20 C01 transcriptions. | Same #7 coverage split as C01. **Partly covered.** | Nothing additional; this row records the third independent attestation of the same list and confirms the reviewer-read Figure 1D transcription is not a misreading. | Record only. Treat C05 as corroboration for C01 and C02; no separate product change. | pending | not started |
| C06: `delve` in Science and Technology podcasts ran about 44% above its synthetic control over months 13 to 18 after the ChatGPT release, 95% CI [+22%, +63%], in-space placebo p = 0.010. Per category: Education +32% (p = 0.059), Business +31% (p = 0.040), category-agnostic +9% (p = 0.218), Sports -7% (p = 0.663). Table S4 reports the same five categories again over the last six recorded months (Nov 2025 to Apr 2026), including Sports at -38%, 95% CI [-68%, +13%], p = 0.208, which C09 does not repeat. | Direct measurement with a stated causal design and a placebo-derived interval. Robust across four control specifications (placebo p from 0.010 to 0.091) and to removing the ten heaviest-using channels. The unit is the fraction of episodes containing the word per month across hundreds of thousands of episodes. | No project mechanism measures frequency change over time, and no check consumes an effect size. `delve` is already a #7 clustering candidate and a Kousha-Thelwall family. **Not covered, and no coverage is applicable.** | Nothing is missing from the product. The value here is that it establishes the direction and dating that C01 and C03's candidate words inherit. | Record only. Preserve the per-category effect sizes and dates as the evidence backing the C01 and C03 additions; no check consumes an effect size and none should be built to. | pending | not started |
| C07: The effect is graded by GPT score rather than vocabulary-wide. Mean post-release slope is +0.030 in the top 1% (n = 36), +0.025 in the top 2%, +0.015 in the top 5%, +0.013 in the top decile, and +0.004 across the whole 3,535-word vocabulary, corresponding to a median peak usage about 50% above the synthetic-control baseline at the top 1%. | Direct measurement against a permutation null obtained by shuffling GPT scores across words; the top-1% mean exceeds twice the null's upper 95% bound. Holds with `delve` excluded (+0.027, permutation p = 0.001) and across all four robustness controls. | No project mechanism. **Not covered, and no coverage is applicable.** | Nothing is missing from the product. This is the selection rationale that justifies bounding any addition at the top 1% rather than reaching further down the score distribution. | Record only. Use C07 as the stated reason the candidate set stops at the top 1%; no threshold or check derives from it. | pending | not started |
| C08: Of the 36 top-1% words, 28 show a post-release increase and 13 credibly so (95% HDI excludes zero); eight decrease, of which two are credibly negative. Fifteen of the 36 showed a credible change in either direction. | Direct measurement from per-word Bayesian change-point fits, four chains of 1,000 post-warmup draws each. This is the paper's own within-set counterexample tally, not an outside criticism. | No project mechanism consumes per-word direction. `underscore`, live in #7, is one of the credible decliners. **Not covered, and no coverage is applicable.** | Nothing is missing from the product. What it changes is how the C03 list should be presented: it is not 36 words that all rose. | Record only. Carry the 28-up, 8-down split into the C03 decision so the list is not described as uniformly rising. No product change of its own. | pending | not started |
| C09: The elevation reverted. The gap peaked around mid-2024, then `delve` fell below its pre-ChatGPT baseline: -15% in Science and Technology, -11% in Education, -30% in Business, and -35% category-agnostic (95% CI [-57%, -7%], two-sided placebo p = 0.05) over November 2025 to April 2026. The turn coincides with the free release of GPT-4o and with growing public awareness of `delve` as an LLM signature. | Direct measurement over the last six recorded months, with the two-sided p flagged by the authors as testing a sign they observed rather than predicted. The corpus runs to 2026-04-30, so this is the most recent dated reading available anywhere in the library for this word. | `delve` is a live `AI_VOCABULARY` entry and a live Kousha-Thelwall family in `grade.py`, both of which were added on evidence from the 2023-2024 rise. Measured on the project corpora on 2026-07-26: `delve` occurs zero times in all 55 human-sourced and all 38 generated-ai samples, so the corpora are silent rather than contradictory. **Challenges current behaviour.** | The catalogue treats `delve` as a current tell with no date attached. The paper measures its use in spontaneous human speech falling below the pre-ChatGPT baseline by early 2026, and separately measures the generator no longer preferring it (C11). Nothing in the product records that. | Documentation plus a decision. Put to Mae whether `delve` and the other reverted words keep unchanged #7 membership, or carry a dated measurement note in #7's `patterns_md_body` in `patterns.json` recording the 2026 reversion. Both options leave the matcher and severity unchanged; there is no check to build here and no removal is proposed. | pending | not started |
| C10: Not all words reverted. `boast`, `meticulous`, and `showcase` maintained a sustained uptake while `delve` and others returned towards baseline. | Direct measurement from the double-sigmoid posterior smoothers in Figure 3A, described in the paper's own Results prose. Three named words, not a quantified subset. | `meticulous` and `showcase` are live `AI_VOCABULARY` entries and live Kousha-Thelwall families; `boast` matches nothing. **Partly covered.** | `boast` and `boasts` are uncovered and are the paper's clearest surviving case: high GPT score, +0.11 slope, and no reversion. | Programmatic. Treat `boast` as the highest-priority single addition inside the C01 and C03 decisions, because it is the one word the paper reports as both strongly preferred and still rising. Same #7 clustering-candidate mechanism and severity. Carry the corpus measurement with it: `boast\w*` runs 4 occurrences across 2 of 55 human documents and 0 across all 38 generated documents, so the samples here do not corroborate the source count and the paper's own measurement is the evidence. Closed 2026-07-26 by DR-165A: DR-165 shipped only the surface form `boasts`, which occurs zero times in either corpus, while the three occurrences the corpora do hold are `boast`, `boasted` and `boasting` and matched nothing. The word-bounded family `boast(?:s\|ed\|ing)?` now covers all four forms as a #7 clustering candidate at unchanged severity. | approved | implemented |
| C11: The generator's own preference attenuated across builds. The `delve` odds ratio in revised arXiv abstracts falls from roughly 380:1 under GPT-3.5-turbo to 100:1 for GPT-4-turbo and 40:1 for GPT-4o. Under GPT-5 (`gpt-5-2025-08-07`) most signature words are no longer over-used and `delve` sits at roughly 0.7:1, below its human baseline. | Direct measurement. Table S1 lists five dated OpenAI API snapshots, but the paper prints an odds ratio for only four of them: GPT-3.5-turbo, GPT-4-turbo, GPT-4o, and GPT-5. No `delve` odds ratio is printed for GPT-4, so the curve is four measured points across five listed builds. GPT-4o and GPT-5 are explicitly excluded from the reference score, so this is reported as drift context rather than folded into the headline result. | H25 exists for exactly this. No live mechanism carries model-version metadata into a check, and #7's vocabulary is undated. **Not covered.** | The eight C01 words already in #7 were justified by GPT-3.5-era evidence, and this paper measures that evidence weakening build by build. | Documentation. Record the five-build attenuation curve, with model snapshot identifiers and dates, in this card and in `pattern-opportunities.md` as H25 evidence. No check consumes model metadata and none is proposed. | pending | not started |
| C12: The effect is one-directional. Words ChatGPT disfavours show no matching decline in spoken use: at the bottom 1% of GPT scores the mean post-release slope is -0.002 against a chance range of [-0.007, +0.014], and the curve stays near zero at every cut-off. | Direct measured null, run as the symmetric counterpart of the headline analysis with the identical permutation machinery. A genuine negative result rather than an absence of testing. | No project mechanism looks for words AI avoids. **Not covered, and no coverage should be built.** | Nothing. This is the argument against a mirror-image mechanism. | Record only. Preserve as a measured null showing that a disfavoured-word check would have nothing to detect; no product change. | pending | not started |
| C13: Word preferences are robust to the rephrasing instruction. The three prompts used (polish, improve, clarify) yield closely similar log-odds ratios for every reference word, with the clarify prompt consistently mildest and never reversing a preference. | Direct measurement (Figure 5a), on arXiv abstracts revised by GPT-3.5-turbo. Three prompts only, all of them edit instructions rather than generate-from-scratch instructions. | No project mechanism. **Not covered, and no coverage is applicable.** | Nothing. It removes a confound that would otherwise limit the C01 and C03 additions to one prompt style. | Record only. Preserve as the prompt-invariance control supporting the C01 and C03 candidate list; no product change. | pending | not started |
| C14: Log-odds magnitudes vary substantially across source corpora, and the paper attributes that variation primarily to baseline differences in human word choice. Its worked example is `underscore`: humans rarely use it in essays, while GPT revisions introduce it frequently across domains including essays. | Direct measurement (Figure 5b and Figure S11) across arXiv, bioRxiv, Nature, Enron emails, Hewlett student essays, Wikipedia, and pre-ChatGPT podcast excerpts. The attribution to human baselines is the authors' reading of the log-probability distributions, not a separate test. | H24 and H12 both exist for register-specific vocabulary behaviour. #7 applies one undifferentiated list at one threshold regardless of register. **Not covered.** | No live mechanism varies vocabulary evidence by register, and this is the first ingested source that measures the same word list across seven named registers including email and student essays. | Record only, as H24 and H12 evidence in `pattern-opportunities.md`. A register-aware threshold is a hypothesis with no design yet, and nothing in this row is buildable as either detector type today. | pending | not started |
| C15: Raw stem counts are contaminated by proper nouns, brands, and transcription artifacts sharing a stem. The named example is `swift`, which sweeps in Taylor Swift, Apple's Swift programming language, the SWIFT banking network, and NASA's Swift telescope. The paper's word-sense audit keeps only curated Wiktionary senses attested at least twice in both corpora and reports that it removes a substantial share of raw matches for contaminated words. | Direct method description with a worked contamination example. The audit is symmetric across human and machine usage and its sense-selection rule never inspects the human-versus-GPT asymmetry, so it does not manufacture the effect. Robustness control C4 re-runs the whole pipeline on un-audited counts and preserves the qualitative pattern. | `AI_VOCABULARY` entries are matched with `re.escape(entry)` and no word boundary, so a bare `swift` entry would match `Swift`, `Taylor Swift`, and `swiftly`; a bare `craft` entry would match `aircraft`, `crafted`, and `Warcraft`; a bare `notice` entry would match `noticed` and `noticeably`. `AI_VOCABULARY_REGEX` does apply `\b` boundaries. Tested live 2026-07-26: `\b(?:swift\|craft\|notice\|creation)\b` against `normalize_for_regex` output correctly rejects `aircraft`, `Warcraft`, `crafted`, and `noticed`, but still matches `taylor swift`, `apple swift`, and `the swift banking network`. **Challenges the naive implementation of C01 and C03.** | Nothing in the product is wrong today, because none of the contaminated words is currently listed. What is missing is a routing rule for the C01 and C03 additions, and one word cannot be routed at all: `check_ai_vocabulary` lowercases each paragraph before calling `_find_ai_words`, which lowercases again inside `normalize_for_regex`, so case is destroyed before any pattern runs and the all-caps SWIFT banking acronym cannot be told from the adjective by any regex in this matcher. | Programmatic, as an implementation constraint on C01 and C03 rather than a separate change. Two mechanically distinct groups. Group one, where word boundaries alone are sufficient and verified: route `craft`, `notice`, `creation`, `spotlight`, `akin`, `catering`, `hinges`, and `alongside` through `AI_VOCABULARY_REGEX` with `\b` rather than through substring-matched `AI_VOCABULARY`. Group two is `swift` alone, which needs a negative lookbehind excluding `taylor ` and `apple ` and still cannot separate the banking acronym while the matcher sees only lowercased text; propose it either with that lookbehind and the residual acronym collision stated, or leave it out of the programmatic set for that reason and let Mae rule. Verify against fixtures containing `Taylor Swift`, `the SWIFT network`, `aircraft`, `Warcraft`, and `noticed` in `dev/evals/tests/test_grade.py`. | pending | not started |
| C16: Ranking words by credible movement alone, without conditioning on GPT preference, recovers a different set. Figure S10's 20 most credibly shifted stems, with slope and GPT-score percentile rank: rises `prompted` +0.21 (0.98), `outperform` +0.23 (0.21), `invaluable` +0.20 (0.94), `gpu` +0.16 (0.60), `python` +0.16 (0.49), `queries` +0.15 (0.72), `vector` +0.14 (0.37), `transcript` +0.13 (0.36), `summarized` +0.13 (0.11), `delve` +0.12 (1.00), `captive` +0.11 (0.88); falls `reynolds` -0.15 (0.38), `twitter` -0.10 (0.51), `mask` -0.15 (0.12), `urine` -0.15 (0.63), `scholars` -0.15 (0.11), `virus` -0.25 (0.70), `corona` -0.40 (0.45), `vaccine` -0.47 (0.55), `pandemic` -0.62 (0.54). | Direct measurement, run as a deliberately preference-blind contrast (Figure S10). The paper presents it as the reason its design conditions on GPT preference first rather than on magnitude of change. After `delve` at rank 1.00, the three next-highest-ranked risers here (`prompted`, `invaluable`, `captive`) are carried separately by C17. | No project mechanism ranks words by movement. **Not covered, and no coverage should be built.** | Nothing. It is the argument against any "which words moved" mechanism, and against reading a rising word as an AI signal without a preference score behind it. | Record only. Preserve as the confound control that makes the C01 and C03 lists usable and that rules out a change-magnitude mechanism. No product change. | pending | not started |
| C17: Setting aside `delve` itself at rank 1.00, `prompted` (slope +0.21, GPT-score rank 0.98), `invaluable` (+0.20, rank 0.94), and `captive` (+0.11, rank 0.88) are the next three highest-GPT-score words among the largest credible risers overall. The paper attributes the post-2022 rise of `prompted` to the new activity of interacting with LLMs. | Direct measurement (Figure S10), with the authors' own referential caveat attached to `prompted` alone: it tracks a new activity rather than a style preference, which is the distinction Figure S10 exists to draw. No such caveat is attached to `invaluable` or `captive`. Note that these three come from the preference-blind panel, so their GPT-score ranks are annotations rather than the selection criterion. | `invaluable` is already a live `AI_VOCABULARY` entry, so `valuable` and `invaluable` both match. `prompted` and `captive` match nothing. Measured on the project corpora 2026-07-26 with `\bprompt(?:s\|ed\|ing)?\b` and `\bcaptiv\w*`, the latter covering `captive`, `captivity`, `captivate`, and `captivating`: `prompted` 3 occurrences in 1 of 55 human documents against 9 in 3 of 38 generated; `captiv*` 27 occurrences in 16 human documents against 2 in 1 generated. **Partly covered.** | `prompted` and `captive` are uncovered high-score risers, and neither is currently proposed anywhere else in this card. | Programmatic. Offer `prompted` and `captive` as #7 clustering candidates on the same mechanism and severity as C01 and C03, with their caveats attached rather than used to withhold them: the paper reads `prompted` as referential vocabulary for a new activity, and the corpus measurement runs `captiv*` roughly 4.5x more often in human prose here than in generated prose per 1000 words. `invaluable` is already covered and needs no change. Rejected 2026-07-26 by DR-165B: bare `prompted` occurs zero times in both sample sets and the wider `prompt` family's 8.9x generated lead names the activity of using a model, which is the referential confound C30's specificity screen exists to exclude; `captiv*` runs 27 occurrences across 16 human documents against 2 across 1 generated. Neither word was added and the matcher is unchanged. | rejected | not applicable |
| C18: The GPT score is a log-odds ratio on document frequency, not token counts, between human-written texts and their model-edited counterparts, Bayesian-marginalised over dataset, model, and prompt with flat Dirichlet priors over 1,000 Monte Carlo samples; the reported score is the median with 95% percentile intervals. Document frequency is chosen because it is immune to within-document repetition and less sensitive to document length. | Direct method description with the formula given. The document-frequency choice is argued rather than asserted, and it is the same measurement basis the project's own #7 counts do not use. | #7 counts occurrences per paragraph, which is a token-count measure, and its document-wide branch counts distinct Kousha-Thelwall families, which is closer to document frequency. **Partly covered as a design parallel; not a claim needing coverage.** | Nothing. The paper's rationale is worth carrying because it explains why a word that repeats in one paragraph is weaker evidence than several distinct words, which is the design #7's two branches already encode. | Record only. Preserve the method and the document-frequency rationale as provenance for the C01 and C03 scores. No product change. | pending | not started |
| C19: Eleven stems were excluded from the score because models repeat them in their own responses rather than in the revised text: `rephrase`, `polish`, `dear`, `text`, `certainly`, `subject`, `readable`, `clarity`, `enhance`, `version`, `title`. | Direct method statement. This is an exclusion list, so the paper supplies no frequency for these words; it is evidence that models emit them, not evidence about how often. | Checked live on 2026-07-26: `enhance` is an `AI_VOCABULARY` entry. `Certainly!` as an opener fails programmatic `no-collaborative-artifacts`. The other nine match nothing. **Partly covered.** | Nine named words the paper reports models emitting are uncovered, and the paper supplies no frequency for any of them because they were excluded rather than scored. | Programmatic, split by whether the word has a surface form in finished prose. Offer `certainly`, `readable`, `polish`, `rephrase`, and `clarity` as #7 clustering candidates on the existing mechanism and severity, with Mae ruling. Attach the exact provenance to each: `polish`, `rephrase`, and `clarity` appear verbatim in the paper's own three prompts (`Please polish this text`, `Please rephrase this text to improve its clarity`), so their frequency is partly instruction-driven, while `certainly` and `readable` appear in no prompt and are the model's own additions. The remaining four cannot be surfaced as prose habits by either detector type for a mechanical reason, not an evidentiary one: `dear`, `subject`, `title`, and `version` are a salutation and three high-frequency common nouns with no distinguishing construction, so any pattern matching them fires on every ordinary use of the same word, and the paper supplies no surrounding form to bound them. `text` is in the same class and also appears verbatim in all three prompts. Ruled in part 2026-07-26 by DR-165A: Mae approved `clarity`, `polish` and `readable` as #7 clustering candidates on the project-corpus measurement rather than on the paper's evidence, `clarity` running 3.7x and `polish` 3.6x more often per 1000 words in generated prose than human and `readable` 3.0x on one occurrence each way. `polish` carries the same lowercasing collision as `swift`, against the nationality, and was added anyway; the corpora contain no nationality use. `certainly` and `rephrase` were rejected 2026-07-26 by DR-165B: `certainly` runs 20 occurrences across 10 human documents against 1 generated, 6.7x the wrong way, and `Certainly!` as an opener already fails programmatic #19; `rephrase` is 1 human occurrence against 0 generated, which is no evidence in either direction. The four unbounded words are unchanged. | approved with changes | implemented |
| C20: Corpus construction and screening: 1,407,131 candidate English podcast episodes published 2017-01-01 to 2026-04-30 drawn from a 4.4-million-series PodcastIndex snapshot dated 2026-04-05, stratified by calendar quarter; 931,450 passed the diarisation dialogue screen and were transcribed with WhisperX large-v3 held fixed across the whole window; 824,634 passed a spontaneity classifier at threshold 3.0, giving 737,083 hours. | Direct method description. The fixed transcription model is the load-bearing control: because it never changes across the time axis, residual transcription error cannot by construction create a change point at the release date. The spontaneity classifier reached about 90% held-out accuracy against annotations with interrater ICC 0.87. | No project mechanism. Relevant only as source metadata. **Not covered, and no coverage is applicable.** | Nothing. It is the scope bound that keeps the C01 and C03 words attributed correctly. | Record only, as corpus scope in this card's metadata; no product change. | pending | not started |
| C21: In the preregistered experiment (N = 496), participants used the AI's assigned synonym variant on 61% of interaction trials against 17% for the alternative (b = 0.44, 95% CI [0.42, 0.46], p < .001); after a three-minute distractor and on entirely new images with no chatbot present, 58% against 23% (b = 0.36, 95% CI [0.33, 0.38], p < .001); and on a forced-choice task 63% (b = 0.13, 95% CI [0.11, 0.14], p < .001). The effect held across nouns, verbs, and adjectives. | Direct measurement, preregistered, randomised, with cluster-bootstrap intervals and Monte Carlo permutation tests. Because assignment to variant was random, the differential adoption isolates the model's causal contribution. Limits: Prolific sample, self-identified native English speakers, single session, spoken descriptions rather than written drafts. | No project mechanism, and none is applicable: human-eyes reads text and never interviews or observes writers. **Not covered.** | Nothing. This is the mechanism claim behind C09 and C24, not a form to detect. | Record only. Preserve as the individual-level mechanism evidence for vocabulary transfer; no check of either type follows from it. | pending | not started |
| C22: Only 15 of 496 participants (3.0%) noticed the chatbot's vocabulary pattern in an open-ended detection check placed before the forced-choice task; most of those attributed the difference to regional or stylistic variation. The remaining 481 reported nothing unusual or noted only incidental features such as response speed or punctuation. | Direct measurement from a preregistered detection check, coded as aware only when a participant named a specific synonym pair or explicitly noted a substitution. The paper reads it as evidence that the shift is implicit. | No project mechanism. **Not covered, and no coverage is applicable.** | Nothing. It reinforces that vocabulary transfer happens below the writer's awareness, which is exactly the case Audit exists to surface for the writer to decide about. | Record only. Preserve as support for the product's surfacing purpose; no product change. | pending | not started |
| C23: The 18 experimental synonym pairs (Table S7). Group A: thermos bottle / vacuum flask, gift / present, cup / mug, to fix / to repair, to look at / to examine, to put up / to install, colorful / multicolored, cracked / fractured, spotted / dotted. Group B: beanie / knit hat, couch / sofa, merry-go-round / carousel, to jump over / to hop over, to hug / to embrace, to cut / to chop, shiny / glossy, round / circular, plaid / checkered. | Direct listing, but these are experimenter-chosen ordinary word pairs selected for familiarity, absence of register difference, and depictability. Which member was the AI variant was randomly assigned and counterbalanced, so both halves served as the AI variant across conditions. | No project mechanism, and none should exist. **Not covered, and no coverage should be built.** | Nothing. This row exists as an explicit guard. | Take no further action, and record the non-promotion reason: none of these 36 words is AI-preferred vocabulary, because the AI variant was assigned at random. They must not enter `AI_VOCABULARY`, `AI_VOCABULARY_REGEX`, or any judgement watchlist. | pending | not started |
| C24: The paper's synthesis: LLM-shaped patterns enter unassisted human production, so human language can no longer be treated as an independent external anchor for the training distribution, and human and machine cultural production form a single integrated system. | Author interpretation built on the paper's own two studies, extended by cited work on model collapse and cultural transmission. The synthesis is argued rather than measured, though its two supporting results are direct. | The root README already states the product's stance on AI writing, and `dev/hypotheses.md` H24 already anticipates public-tell drift. **Partly covered as framing.** | The project has no dated statement that the vocabulary it checks is measurably entering ordinary human production. | Record only, in this card and as H24 evidence in `pattern-opportunities.md`. The concrete, dated consequence for #7 is carried by C09 rather than by this framing row; no separate product change. | pending | not started |
| C25: Words that become culturally legible as AI-associated may attract avoidance after initial uptake, as speakers distance themselves from forms that threaten authenticity, in a pattern the paper likens to hypercorrection. It adds that `delve` may come to be stereotypically associated with lower skill or uncritical AI use. | Author interpretation offered as a plausible sociolinguistic mechanism for the measured C09 reversion, supported by citation to Bourdieu and Labov rather than by a test in this paper. Explicitly speculative on the stigma point. | The library already holds journalism on AI-shaming and camouflage (`slate-ai-shaming-paranoia.md`, `phillips-ringer-em-dash.md`, `csutoras-em-dash-dilemma.md`). **Partly covered by existing cards as context.** | Nothing buildable. The product surfaces overused habits for the writer to decide about; it does not model reader stigma. | Record only, as the proposed mechanism behind the C09 reversion. No product change; it is an interpretation, not a form. | pending | not started |
| C26: Stated limits. The corpus is English-only and covers a self-selected, public-facing population of podcast hosts and guests. The main analysis covers the first 18 months after release and treats OpenAI's models as the dominant driver, while deployment has since fragmented. The no-interference (SUTVA) assumption is named as binding and cannot be guaranteed, though donors were restricted to near-zero-GPT-score words with the closest semantic neighbours excluded. The experiment used Prolific-recruited self-identified native English speakers in a single session after a brief distractor. | Direct statements by the authors in their own Discussion, not reviewer criticism. They are the scope bounds that any use of C01 and C03 inherits. | The project's source-metadata convention already requires corpus dates, language, genre, and model versions on cards. **Partly covered by convention.** | Nothing. These bounds belong in the card's metadata, which they now occupy. | Record only. Carry the limits into the card metadata and into any presentation of C01 and C03 so the word list is not described as language-general or open-ended in time. | pending | not started |
| C27: The paper distinguishes written from spoken evidence. In writing, a model-preferred word can enter a text without ever entering the writer's vocabulary, through full production or copy-paste; spontaneous speech excludes those shortcuts, so a word surfacing in unscripted talk signals internalisation. The paper notes that the written footprint was already documented, with `delve` rising across scientific abstracts and peer reviews soon after the release. | The written-footprint statement is cited from Liang et al. and is indirect here. The written-versus-spoken argument is the authors' own reasoning. The load-bearing point for this project is direct: the GPT score itself is measured on written prose and its model-edited counterpart, so the word list is a written-text measurement and only the outcome is speech. | The library currently records this work as spoken-register evidence and therefore out of scope for written-prose vocabulary decisions, in `fairbanks-atlantic-ai-writing.md` C17 and `slate-ai-shaming-paranoia.md` C09. **Challenges current behaviour.** | The existing indirect records treat the whole paper as spoken-language evidence. Reading the source directly shows the word list is derived from written human prose against its GPT-edited counterpart, which is the register human-eyes reads; the podcast corpus is the outcome variable. | Documentation. Correct the register framing in this card and in the indirect cards named under C29, so the C01 and C03 additions are understood as resting on a written-prose measurement rather than on a spoken-corpus result. No check changes. | pending | not started |
| C28: Appendix B replicates the population-level signal on 360,445 academic YouTube talks from the channels of 20,622 research institutions. The `delve` placebo test gives p = 0.010, and `comprehend`, `boast`, `swift`, and `meticulous` all show a significant post-release uptake. | Direct measurement on a second corpus, with the diarisation and spontaneity steps omitted because academic talks are predominantly prepared monologic speech. The paper positions it as a complement sitting between the written academic record and spontaneous conversation. | `meticulous` matches #7; `comprehend`, `boast`, and `swift` match nothing. **Partly covered.** | Three of the four words the replication names are uncovered, and they are the same three the main analysis names. | Programmatic, folded into C01 and C03 rather than ruled separately: record the YouTube replication as independent corroboration for `comprehend`, `boast`, and `swift`, which strengthens their case within the same #7 clustering-candidate addition. | pending | not started |
| C29: The reviewed v4 supersedes the v3 figures the library cites second-hand. v3's abstract reported 740,249 hours across 360,445 YouTube academic talks and 771,591 conversational podcast episodes and named `delve, comprehend, boast, swift, and meticulous`. v4 reports 737,083 hours from 824,634 spontaneity-screened podcast episodes drawn from 1,407,131 candidates, demotes the YouTube corpus to an Appendix B replication, adds a preregistered N = 496 experiment and a reversion result, extends the corpus to 2026-04-30, and names `delve, showcase, boast, intricacies and meticulous`. | Provenance finding established by this ingestion, not a claim the paper makes. Verified against the v3 abstract and the arXiv submission history (v1 2024-09-03, v2 2025-06-30, v3 2025-07-08, v4 2026-07-16), both retrieved 2026-07-26 and both preserved in the snapshot under `Prior version compared`. The comparison is abstract-level; no v3 full text was retrieved. | Four cards cite this work second-hand, but at different depths. Only `fairbanks-atlantic-ai-writing.md` C17 states the v3 corpus figures (740,249 / 360,445 / 771,591). `chitalian-offensive-ai-speak.md` C17 names the v3 version tag and carries a separate unverified `delve` figure. `slate-ai-shaming-paranoia.md` C09 cites the bare arXiv ID with no version and no figures. `kriss-nyt-ai-write-like-that.md` C21 names neither the authors nor the ID, only "more than 360,000 academic YouTube videos". All four carry a pending follow-up asking whether this paper should be ingested directly (fairbanks line 91, slate line 68, kriss line 74, chitalian line 85). Two preserved snapshots also cite the work (`wikipedia-signs-of-ai-writing.md`, `abdulhai-llms-distort-written-language.md`); snapshots are point-in-time records and must not be edited. **Challenges current records.** | The four cards' pending "ingest Yakura directly" question is now answered and none of them says so. Separately, `chitalian-offensive-ai-speak.md` C17 records an inherited claim that human use of `delve` rose by more than 50 per cent and states it cannot be verified without a direct card; this paper now supplies the measurement. | Documentation. Update the four cards to cite this card as the direct record and close their pending "ingest Yakura directly" follow-ups; mark the fairbanks C17 corpus figures and the chitalian C17 version tag as v3, the prior version. Resolve the chitalian C17 `delve` figure against the source's actual numbers: +44% over the synthetic control in Science and Technology across months 13 to 18 (Table S4) and a median peak about 50% above the synthetic-control baseline at the top 1% of GPT scores (Figure 3 caption), so the inherited "more than 50 per cent" overstates the per-category result and roughly matches the top-1% median peak. The two snapshots stay untouched as preserved records. No check changes and no decision-register edit is made here. | pending | not started |
| C30: The paper's specificity argument: the top-1% words have no obvious referential connection to language models and occupy a frequency band in which natural-rate lexical change is slow, unlike the COVID-19 vocabulary (`pandemic`, `vaccine`, `mask`) and machine-learning vocabulary (`gpu`, `python`, `vector`) its own corpus also shows moving, and unlike `prompted`, which tracks the new activity of interacting with LLMs. | Author argument, resting on the paper's own measured C16 contrast plus a cited result on rates of semantic change. It is the test that separates a style preference from topical churn. | No project mechanism. **Not covered, and no coverage is applicable.** | Nothing. It supplies the screen that keeps `gpu`, `python`, `vector`, `queries`, and `transcript` out of any candidate list built from this paper, and it is the qualification C17 attaches to `prompted` rather than a reason to withhold it. | Record only. Preserve as the selection criterion applied to C01, C03, and C17; no product change. | pending | not started |

## Recommendations

- C01 and C03 approved with changes 2026-07-26 via DR-165: 26 of the paper's named words ship as #7 clustering candidates, which never fire alone. Measured on the project corpora first: 16 of the 27 not already carried appear in neither sample, 9 of the 11 that do appear run in the paper's direction, and `hinges` and `comprehend` run against it on one and two occurrences. Mae ruled to add them regardless, since a clustering candidate that never appears costs nothing. `swift` was excluded because `_find_ai_words` lowercases before matching and cannot separate the adjective from `Taylor Swift`, `Apple Swift`, or the SWIFT acronym. `craft`, `creation`, `notice`, `impressive`, `thorough` and `akin` went to `AI_VOCABULARY_REGEX` with word boundaries rather than the plain substring list; `akin` alone appears inside making, taking and speaking 164 times across the corpora. The measured effect on the corpora is one extra human document flagged and no extra generated ones.
- C01: Twenty-six of the named words are now #7 clustering candidates; `swift` was excluded and twelve were already carried.
- C02: Record only as text-attested corroboration of the C01 transcription. **Decision:** pending. **Status:** not started.
- C03: Twenty-six of the named words are now #7 clustering candidates; `swift` was excluded and twelve were already carried.
- C04: Programmatic. Offer the twelve credible-change words as the defensible narrow alternative to C01 and C03; eight already match, so only `boast`, `alongside`, `hinges`, and `surpassed` would be added. Record `underscore`'s negative direction with it. **Decision:** pending. **Status:** not started.
- C05: Record only as the third independent attestation of the same word list. **Decision:** pending. **Status:** not started.
- C06: Record only. Preserve all five per-category effect sizes across both Table S4 windows, including the Sports readings, as evidence backing C01 and C03; build nothing that consumes an effect size. **Decision:** pending. **Status:** not started.
- C07: Record only. Preserve as the stated reason the candidate set stops at the top 1% of GPT scores. **Decision:** pending. **Status:** not started.
- C08: Record only. Carry the 28-up, 8-down split into the C03 decision. **Decision:** pending. **Status:** not started.
- C09: Documentation plus a decision. Ask Mae whether `delve` and the other reverted words keep unchanged #7 membership or gain a dated measurement note in #7's `patterns_md_body` in `patterns.json`. Matcher and severity unchanged either way. **Decision:** pending. **Status:** not started.
- C10: Programmatic. Treat `boast` as the highest-priority single addition within C01 and C03, being the one word reported as both strongly preferred and still rising. **Decision:** approved 2026-07-26 via DR-165A. **Status:** implemented.
- C11: Documentation. Record the GPT preference attenuation as H25 evidence in `pattern-opportunities.md`: four measured `delve` odds ratios across the five dated API snapshots Table S1 lists, with no ratio printed for GPT-4. **Decision:** pending. **Status:** not started.
- C12: Record only as a measured null against building a disfavoured-word mechanism. **Decision:** pending. **Status:** not started.
- C13: Record only as the prompt-invariance control supporting C01 and C03. **Decision:** pending. **Status:** not started.
- C14: Record only as H24 and H12 evidence in `pattern-opportunities.md`; nothing here is buildable as either detector type today. **Decision:** pending. **Status:** not started.
- C15: Programmatic, as an implementation constraint on C01 and C03. Route `craft`, `notice`, `creation`, `spotlight`, `akin`, `catering`, `hinges`, and `alongside` through `AI_VOCABULARY_REGEX` with `\b` boundaries rather than substring-matched `AI_VOCABULARY`, which is verified to reject `aircraft`, `Warcraft`, `crafted`, and `noticed`. Handle `swift` separately: it needs a negative lookbehind excluding `taylor ` and `apple `, and even then cannot separate the all-caps SWIFT banking acronym, because the matcher lowercases before any pattern runs. Propose it with that residual collision stated or leave it out for that reason, and let Mae rule. Add fixtures containing `Taylor Swift`, `the SWIFT network`, `aircraft`, `Warcraft`, and `noticed` to `dev/evals/tests/test_grade.py`. **Decision:** pending. **Status:** not started.
- C16: Record only, preserving all 20 Figure S10 stems with their slopes and GPT-score ranks, as the confound control against any change-magnitude mechanism. The three next-highest-ranked risers after `delve` are proposed under C17. **Decision:** pending. **Status:** not started.
- C17: Programmatic. Offer `prompted` and `captive` as #7 clustering candidates on the same mechanism and severity as C01 and C03, with their caveats attached rather than used to withhold them: the paper reads `prompted` as referential vocabulary for a new activity, and `captiv*` measures roughly 4.5x more common in this project's human corpus than its generated one. `invaluable` is already covered and needs no change. **Decision:** rejected 2026-07-26 via DR-165B; `prompted` is silent in both corpora and its family lead is referential, and `captiv*` runs 4.5x the wrong way. **Status:** not applicable.
- C18: Record only. Preserve the log-odds method and its document-frequency rationale as provenance for the C01 and C03 scores. **Decision:** pending. **Status:** not started.
- C19: Programmatic. Offer `certainly`, `readable`, `polish`, `rephrase`, and `clarity` as #7 clustering candidates, recording that the last three appear verbatim in the paper's own prompts while the first two do not; `enhance` is already covered and `Certainly!` as an opener already fails `no-collaborative-artifacts`. State the mechanical reason the other five cannot be surfaced by either detector type: `dear`, `subject`, `title`, `version`, and `text` are a salutation and four high-frequency common nouns with no distinguishing construction, so any pattern matching them fires on every ordinary use. **Decision:** approved in part 2026-07-26 via DR-165A; Mae added `clarity`, `polish` and `readable` on the project-corpus measurement. `certainly` and `rephrase` were rejected via DR-165B, running 6.7x the wrong way and supplying one occurrence respectively. **Status:** implemented.
- C20: Record only as corpus scope in this card's metadata. **Decision:** pending. **Status:** not started.
- C21: Record only as individual-level mechanism evidence; no check of either type follows. **Decision:** pending. **Status:** not started.
- C22: Record only as support for the product's surfacing purpose. **Decision:** pending. **Status:** not started.
- C23: Take no further action, and record the explicit non-promotion: the 18 experimental synonym pairs are counterbalanced ordinary words and must never enter `AI_VOCABULARY`, `AI_VOCABULARY_REGEX`, or a judgement watchlist. **Decision:** pending. **Status:** not started.
- C24: Record only, in this card and as H24 evidence in `pattern-opportunities.md`; the dated product consequence is carried by C09. **Decision:** pending. **Status:** not started.
- C25: Record only as the proposed sociolinguistic mechanism behind the C09 reversion. **Decision:** pending. **Status:** not started.
- C26: Record only. Carry the stated limits into the card metadata and into any presentation of C01 and C03. **Decision:** pending. **Status:** not started.
- C27: Documentation. Correct the register framing here and in the indirect cards named under C29, so C01 and C03 are understood to rest on a written-prose measurement. **Decision:** pending. **Status:** not started.
- C28: Programmatic, folded into C01 and C03. Record the YouTube replication as independent corroboration for `comprehend`, `boast`, and `swift`. **Decision:** pending. **Status:** not started.
- C29: Documentation. Update the four cards citing this work second-hand to point at this card as the direct record and close their pending "ingest Yakura directly" follow-ups; mark the fairbanks C17 corpus figures and the chitalian C17 version tag as v3. Resolve chitalian C17's inherited "more than 50 per cent" `delve` claim against the source's actual numbers, +44% per category and about 50% median peak at the top 1%. The two preserved snapshots stay untouched. **Decision:** pending. **Status:** not started.
- C30: Record only as the selection criterion applied to C01 and C03, and as the qualification C17 attaches to `prompted`. **Decision:** pending. **Status:** not started.

## Evaluation of approved changes

- C01: passed - DR-165A extended this to word-bounded inflection families; `python3 dev/evals/tests/test_grade.py` pins all 82 inflected forms across 29 families, clears 19 stems against host words, and holds the `swift` exclusion. Measured on both corpora: #7 moves from 11%/5% of human/generated documents to 11%/8%.
- C01 (first pass): passed - `python3 dev/evals/tests/test_grade.py` pins all 26 as recognised candidates, pins `swift` out of `AI_VOCABULARY`, and pins the six boundary-sensitive entries against host words (aircraft, craftsmanship, recreational, noticeably, unimpressive, thoroughly, making, taking) while still matching as whole words. Measured on both corpora: #7 moves from 10%/7% of human/generated documents to 11%/7%.
- C02: not applicable - record-only recommendation pending.
- C03: passed - DR-165A extended this to word-bounded inflection families; see the C01 evaluation line for the pinned coverage and the measured corpus effect.
- C03 (first pass): passed - `python3 dev/evals/tests/test_grade.py` pins all 26 as recognised candidates, pins `swift` out of `AI_VOCABULARY`, and pins the six boundary-sensitive entries against host words (aircraft, craftsmanship, recreational, noticeably, unimpressive, thoroughly, making, taking) while still matching as whole words. Measured on both corpora: #7 moves from 10%/7% of human/generated documents to 11%/7%.
- C04: not applicable - recommendation pending; no product change made during this ingestion.
- C05: not applicable - record-only recommendation pending.
- C06: not applicable - record-only recommendation pending.
- C07: not applicable - record-only recommendation pending.
- C08: not applicable - record-only recommendation pending.
- C09: not applicable - documentation recommendation pending; `patterns.json` untouched.
- C10: passed - `boast(?:s\|ed\|ing)?` is pinned as a recognised #7 clustering candidate by the DR-165A family assertions in `dev/evals/tests/test_grade.py`; all four forms match and `boastful` stays clear.
- C11: not applicable - documentation recommendation pending.
- C12: not applicable - record-only recommendation pending.
- C13: not applicable - record-only recommendation pending.
- C14: not applicable - record-only recommendation pending.
- C15: not applicable - implementation-constraint recommendation pending; `grade.py` untouched.
- C16: not applicable - record-only recommendation pending.
- C17: not applicable - rejected 2026-07-26 via DR-165B; no product change.
- C18: not applicable - record-only recommendation pending.
- C19: passed - `clarity`, `polish(?:es\|ed\|ing)?` and `readable` are pinned as recognised #7 clustering candidates by the DR-165A family assertions in `dev/evals/tests/test_grade.py`; `unreadable`, `polishable` and `clarification` stay clear.
- C20: not applicable - record-only recommendation pending.
- C21: not applicable - record-only recommendation pending.
- C22: not applicable - record-only recommendation pending.
- C23: not applicable - explicit non-promotion pending.
- C24: not applicable - record-only recommendation pending.
- C25: not applicable - record-only recommendation pending.
- C26: not applicable - record-only recommendation pending.
- C27: not applicable - documentation recommendation pending.
- C28: not applicable - recommendation pending; no product change made during this ingestion.
- C29: not applicable - documentation recommendation pending; the four cited cards are unchanged.
- C30: not applicable - record-only recommendation pending.

## Document review

- **Review status:** passed
- **Review method:** independent source-record reviewer: af3bce0b82522c794
- **Reviewer isolation:** fresh source-dedicated agent; one source only; not reused
- **Findings resolved:** Twenty-six findings across three rounds, all resolved. Provenance: the MANIFEST row was rewritten so the card's exact `Extraction method` and `Stable identifier` strings appear in it, as the validator requires; the v3 abstract and the full arXiv submission history are now preserved in the snapshot under `Prior version compared`, so C29 no longer rests on an unrecorded retrieval; the snapshot's stray control bytes are restored to the glyphs they stand for rather than deleted, after the reviewer showed that all 488 decode to `space`, `-`, `.`, `/`, `0`, `1`, `2`, `5`, and `7` under the same broken font subset and that the earlier claim of full preservation was false, with only the 2,337 glyph-free NUL padding bytes dropped and Poppler's form feeds replaced by explicit page markers. Source fidelity: the Figure 3A transcription was corrected against a fresh render (`surpassed` and `underscore` were transposed), the Figure 3B label order was added, and the ordering identity that Table S2's caption asserts is now recorded as the paper's own inconsistency rather than repeated as fact. Coverage: C04's covered count was corrected from nine to eight, C15's routing was rewritten after the reviewer showed word boundaries do not clear `taylor swift`, `apple swift`, or the SWIFT acronym and that `check_ai_vocabulary` destroys case before any pattern runs, and `swift` was separated from the boundary-sufficient group in both C01 cells. Completeness: `captive` was added to C16, C17, and Main insights; C16 now carries all 20 Figure S10 stems with slopes and ranks; C06 gained the Sports recent-six reading; C11 was corrected to four measured odds ratios across five listed builds; C29 now distinguishes the four citing cards by depth and names the `delve` figure it resolves in the chitalian record. Decision integrity: C17 and C19 were rewritten from no-action dispositions into typed programmatic proposals with their qualifications attached, `rephrase` was restored to C19's set, and the contradicting sentence in C30 and two stale evaluation lines were corrected. Measurement: every proposed word was measured against the project corpora, with the tokeniser and the inflection-only family boundaries named.
- **Unresolved findings:** none
