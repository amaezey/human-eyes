# Synergizing Stylometrics with Semantics: Dual-Path Framework for LLM Detection and Attribution

## Metadata

- **URL:** https://aclanthology.org/2026.findings-acl.1855/
- **Author / owner:** Xingyu Lu, Yumeng Ma, Xiang Zhou, Shengli Gan, Guiying Deng, Yang Wen, and Yanbing Liu
- **Published:** 2026-07; proceedings dates July 2-7, 2026
- **Retrieved:** 2026-07-15
- **Extracted:** 2026-07-15
- **Source type:** peer-reviewed empirical research
- **Evidence tier:** Peer-reviewed / academic empirical
- **Review mode:** new
- **Stable identifier:** DOI 10.18653/v1/2026.findings-acl.1855; ACL Anthology ID 2026.findings-acl.1855
- **Version / revision:** Findings of ACL 2026 proceedings version; ACL Anthology landing page commit 717fd3cc0c5b0c9b4c55108aae362c2f8dc47bd0
- **Full-text status:** complete
- **Snapshot:** `snapshots/lu-et-al-stylometrics-semantics.md`
- **Extraction method:** Official ACL paper and checklist PDFs downloaded directly; all pages converted with Poppler `pdftotext -layout`; page counts checked with `pdfinfo`; paper pages 1, 8, and 14 plus both checklist pages rendered and visually compared with the extraction.
- **Snapshot SHA-256:** `91f46741086676a1a1f97fdc2902396ee5545beb590e18c31d33b6d712e4cf47`
- **Model / corpus scope:** English MGTBench Essay, Reuters, and Wikipedia datasets containing human text and text from six LLMs for seven-way closed-set attribution and binary human-versus-machine detection. The paper names ChatGPT, Claude, and LLaMA as examples but does not enumerate all six source models or report their exact versions. DeepSeek-V3 is the default rewriting probe, with Gemini-1.5-Flash as an alternate. A separate XSum persona-steering test uses GPT-4o, DeepSeek-V3, Gemini-1.5-Flash, and GLM-4. Training subsets range from 839 to 8,397 samples.
- **Access limitations:** No pages or supplements were inaccessible. The six main benchmark generators and their exact versions are not fully enumerated in the paper text, code and trained artifacts are not linked from the ACL record, and most reported results do not state repeated-run variance. PDF column layout, tables, formulas, and figure labels were transformed through the embedded text layer, with selected rendered pages checked visually.

## Summary

This Findings of ACL 2026 paper proposes Stylometric-Semantic LLM Attribution, or SSLA, a supervised dual-path classifier that combines handcrafted lexical, syntactic, structural, and rewrite-based features with RoBERTa embeddings. It evaluates seven-way source attribution and binary detection on three English MGTBench domains, plus short-text, low-data, cross-domain, alternate-probe, and persona-steering conditions. The paper provides useful evidence that model-specific style signals can complement semantic representations, but it does not validate any one feature as a universal AI-writing tell or an individual-document authorship rule. The user queue title, "Synergizing Stylometrics with Semantics", is incomplete; the authoritative title at the supplied URL adds the subtitle "Dual-Path Framework for LLM Detection and Attribution".

## Main insights

- The authors report model-dependent lexical, syntactic, and semantic-intention differences under matched prompts, but the exploratory sample size and feature-direction statistics are not reported.
- SSLA's explicit feature path contains type-token ratio, sentence length, noun and verb ratios, average dependency depth and distance, syntax-semantic n-grams, and six similarities between an input and an LLM rewrite.
- The main strength comes from the semantic path and learned fusion. The combined static-plus-rewrite Feature variant, rewrite-only variant, and SN-Gram-only variant perform much worse than RoBERTa or the complete system. Adding the Feature block directly to RoBERTa also reduces average F1, so the study does not justify promoting isolated surface metrics as rules.
- In-domain seven-way attribution is strong, with Macro-F1 values of 0.945 on Essay, 0.966 on Reuters, and 0.956 on Wikipedia, but OTB-D is slightly higher on Essay at 0.949. These are classifier results on a closed benchmark and are outside human-eyes' product objective.
- Short-text, low-data, cross-domain, and persona-steering tests support separate robustness evaluation. They do not establish a universal feature threshold.
- The data-efficiency prose calls the plotted result accuracy, while Figure 3 labels the vertical axis Macro-F1 Score. The metric is internally inconsistent and should not be reported as either one without qualification.
- The cross-domain result, 0.7083 Macro-F1 from Reuters training to Essay testing, is materially below the in-domain results even though it exceeds the compared baselines.
- Replacing DeepSeek-V3 with Gemini-1.5-Flash changes individual dataset results while leaving the reported average close, so rewrite-based rigidity is not fully probe-free.
- A human case is misclassified after the semantic path overpowers a correct style-path decision, while a ChatGLM case needs the semantic path to correct a style-path ChatGPT label. These cases show that familiar constructions and summary metrics remain ambiguous.
- The paper is limited to English, high-quality parsing, offline closed-set attribution, and known source classes. Unknown models remain untested.
- The official checklist reports limited AI assistance for grammar, clarity, wording, and submission procedure, with no role claimed in the ideas, method, findings, or substantive content. It also states that the paper has no dedicated downstream-risk discussion.

## Evidence and claims to extract

- **Direct source reviewed:** The 14-page Findings of ACL 2026 proceedings PDF, DOI 10.18653/v1/2026.findings-acl.1855, and its official two-page Responsible NLP Checklist, both retrieved from ACL Anthology on 2026-07-15.
- **Method and sample:** SSLA is trained for seven-way attribution over human text and six LLM sources in English MGTBench Essay, Reuters, and Wikipedia data. It uses a 140-dimensional style vector comprising six static linguistic metrics, six rewrite-comparison metrics, and 128 selected syntax-semantic n-gram features, plus a RoBERTa-base semantic path. The paper reports binary detection, dataset subsets from 839 to 8,397 samples, Reuters-to-Essay transfer, length strata including fewer than 50 tokens, alternate rewriting probes, and a four-model XSum persona-steering test.
- **Direct versus cited evidence:** C01 through C13 distinguish the paper's own methods, tables, ablations, case studies, limitations, and checklist responses from cited background. The general claim that LLM style fingerprints persist across prompted styles is also attributed to Bitton et al. 2025. Claims about prior detectors, literary attribution, RLHF bias, and earlier stylometric methods remain indirect and are not promoted here.
- **Important limits and counterexamples:** The work is English-only, closed-set, parser-dependent, computationally heavier than a semantic classifier, and evaluated on public benchmark data. Exact versions for the six main generators are not reported. The paper supplies no open-world result, no deployment-risk analysis, no direct human-subject study, no universal direction or threshold for its individual linguistic features, and a qualitative case where fusion turns a correct style-path human label into an error. Figure 3 conflicts with its surrounding prose about whether it reports accuracy or Macro-F1, and SSLA does not lead every dataset-level comparison in Tables 1, 3, or 5.

## Skill-use audit

- **Good use:** Support H22 and H25 research on structural feature families and model-specific residue; support a separate robustness lane for length, data volume, domain transfer, probe choice, and adversarial persona constraints; reinforce the rule that multiple weak signals require context and controls.
- **Misuse / overclaim:** Do not turn noun ratio, verb ratio, dependency depth, sentence length, type-token ratio, n-grams, or rewrite similarity into universal AI-writing rules from this paper. Do not import SSLA accuracy or Macro-F1 values as human-eyes thresholds or imply that the current signal-stacking check reproduces learned dual-path fusion.
- **Unsupported use:** The source cannot support authorship verdicts for an arbitrary document, low-resource languages, unknown generators, open-world detection, real-time streaming, a particular current model version outside the named tests, or a claim that style alone is sufficient.
- **Underused evidence:** The current evaluation method does not stratify results by text length, training volume, rewriting probe, or deliberate persona constraint, and it does not preserve a model-family attribution research lane separate from generic pattern accumulation.
- **Patterns left on the table:** Dependency depth and distance, noun and verb ratios, syntax-semantic n-grams, rewrite-based rigidity, and learned semantic-style interaction are absent from active checks. They should remain research candidates until matched human controls, cross-condition stability, and product-boundary questions are resolved.

## Matched patterns / rules

- `vocabulary-diversity` and pattern B5: partial overlap with type-token ratio only.
- `sentence-length-variance` and H13: related length features, but the paper uses sentence length and length strata rather than the current within-document standard-deviation rule.
- `overall-signal-stacking`: conceptual overlap with combining weak signals, but not implementation equivalence to supervised attention fusion.
- `genre_specific`, H12, H22, H23, and H25: domain, grammar-feature, and model-family context.
- `STRATEGY.md`: the paper's classifier objective remains outside human-eyes' non-authorship product boundary.
- `dev/TESTING.md`: matched samples, provenance, weak and reversed cases, and separate handling of coached or adversarial samples.

## Associated hypotheses

- H1: Continuous calibrated register-distance score per pattern.
- H3: Drop detection framing entirely.
- H12: Genre-aware threshold calibration.
- H13: Sentence-length mean as a grader check.
- H22: Long-tail compression and grammatical standardisation.
- H23: Nominalization and noun-heavy style.
- H25: Model-family versus generic-AI residue.

## Questions / follow-up

- Which exact six generators and versions make up the main MGTBench experiments? The paper gives examples but no complete versioned list.
- Are code, splits, trained weights, seeds, and per-run results available from an author repository not linked by ACL Anthology?
- Do the individual style features retain direction and effect size across matched human controls, newer generators, additional prompts, and non-English parsers?
- Would the maintainer approve a separate, explicitly non-product research lane for rewrite-probe and closed-set attribution evidence, or should these results remain card-only context?
- The cited Bitton et al. style-fingerprint paper is indirect evidence here. Ingest it separately before using its prompted-style persistence claim for a product recommendation.

## Update provenance

Not applicable: initial ingestion.

## Decision history

- DR-80, DR-81 rejected 2026-07-26: needs a second model to rewrite the text and compare, and is framed as attribution; a detector robustness matrix, not a writing pattern. No checker, registry, or test change was made.
- 2026-07-26: C02 approved and C03 rejected via DR-79. The paper names six static features and supplies a direction for none of them, so every direction was measured on the project corpora. Type-token ratio was already B5. Sentence-length spread exposed a dead check: G9's inherited standard-deviation threshold of 4 sat below the entire observed range, the flattest document in either corpus reaching 4.4, so it had never fired on any of the 108 documents, while the measure separates them at a median 12.3 human against 7.6 generated. Mae approved retuning it to 9.0, which flags 72% of generated documents and 11% of human ones. She also approved folding mean sentence length into E5 at 15.0 words in prose of 300 words or more, generated prose averaging 13.9 words per sentence against 17.7 human. Noun ratio, verb ratio, average dependency depth, and average dependency distance were re-queued as POS-03 to POS-06 against her tagger; the dependency pair needs a parser rather than a tagger alone, and none of the four has a defensible surface proxy. The 128 syntax-semantic n-gram features are learned model inputs and cannot be enumerated from the publication. The rewrite-rigidity vector was rejected as not a text pattern.

None: initial review.

## Project coverage

This is the authoritative review table. Give every relevant source claim or example a stable claim ID in the first cell.

Escape any literal pipe within a table cell as `\|`; an unescaped extra pipe makes the row invalid.

| Source claim or example | Evidence assessment | Existing project coverage | Missing or challenged | Recommendation | User decision | Implementation status |
|---|---|---|---|---|---|---|
| C01: Under identical prompts, human and model outputs show model-dependent differences across lexical choice and function words, syntactic organisation, and semantic framing. The authors describe these as persistent multi-level style fingerprints. | Direct exploratory analysis and author interpretation, with persistence also supported indirectly through Bitton et al. 2025. The paper does not report the exploratory sample count, feature distributions, effect sizes, or all six generator versions. This supports a research hypothesis, not a document rule. | partly covered: H22 and H25 already track grammatical compression and model-family residue. `vocabulary-diversity` covers coarse type-token ratio, while `sentence-length-variance` covers rhythm dispersion. | Current checks do not identify model-specific fingerprints, function-word distributions, dependency organisation, or semantic framing as a combined profile. | If approved, cite this source under H22 and H25 as closed-set model-family evidence and require matched human controls plus generator, version, prompt, domain, and length metadata. Do not change active checks. Verify through a preregistered multi-model comparison. | pending | not started |
| C02: SSLA's explicit feature path contains a six-dimensional static linguistic block with average dependency depth, average dependency distance, type-token ratio, sentence length, noun ratio, and verb ratio, plus 128 selected syntax-semantic n-gram features. | Direct methodology. These are classifier inputs selected for supervised attribution, not individually validated universal AI indicators. Feature directions and standalone thresholds are mostly absent. | partly covered: `vocabulary-diversity` implements type-token ratio at 150 or more words and `sentence-length-variance` measures within-document sentence standard deviation. H13, H22, and H23 track mean length, structural tails, and noun-heavy style. Focused checks on the paper's two case-study passages passed both current metrics: human TTR 0.675 and sentence SD 11.5; ChatGLM TTR 0.572 and sentence SD 6.1. | No active noun or verb ratio, dependency depth or distance, sentence-mean, function-word, or syntax-semantic n-gram feature exists. Current metrics are not SSLA equivalents and the source does not justify fixed thresholds. | If approved, add a research-only feature matrix to H22, H23, and H25. Test each metric across matched registers and models with uncertainty and multiple-feature controls before considering product behaviour. | approved | implemented |
| C03: The rewrite-rigidity vector compares the input with a semantic-preserving rewrite using BLEURT, BERTScore, SBERT cosine, RoBERTa cosine, CHRF++, and surface cosine; the authors hypothesise more drift for human text and more rigidity for aligned LLM text. | Direct method and rationale. Ablations show the vector contributes discriminative information, but the paper does not report a direct human-versus-LLM distribution or a safe threshold for any similarity. The probe itself is another LLM. | not covered: `human-eyes/references/process.md` checks whether a rewrite preserves the source, but it does not use an LLM rewrite as an authorship feature. The active Audit has no rewrite-probe metric. | The method would add external-model cost, version drift, circularity, and a new authorship-classification objective. It is not validated for human-eyes' editing use case. | Record as research context only. If the user later approves exploration, isolate it outside the product path, use multiple versioned probes, preserve every prompt and output, include human and AI controls, and test whether the signal survives probe and genre changes. | rejected | not applicable |
| C04: On seven-way in-domain attribution, SSLA reports Macro-F1 of 0.945 on Essay, 0.966 on Reuters, and 0.956 on Wikipedia; binary results reach 0.993, 0.997, and 0.974 respectively. | Direct Tables 1 and 3. These are supervised closed-set benchmark results against named baselines. SSLA does not lead every comparison: OTB-D has higher seven-way Essay Macro-F1 at 0.949, and higher binary Macro-F1 on Essay at 0.996 and Wikipedia at 0.977. The paper's broad state-of-the-art language therefore does not hold for every table cell. The six main generator versions and repeated-run protocol are not fully reported, and the metrics do not transfer to prose-pattern auditing. | not covered by design: `STRATEGY.md` states that human-eyes does not determine whether a human or AI wrote a document. `dev/TESTING.md` measures pattern accumulation and rewrite quality, not classifier accuracy. | Importing the scores would misstate the product goal and hide the closed-set benchmark conditions. | Record the results and boundary only. Do not import the scores, thresholds, or authorship labels into human-eyes. No product verification is applicable. | pending | not started |
| C05: On Reuters texts under 50 tokens, SSLA stays above 0.80 F1 while OTB-D falls below 0.30, although the authors still call ultra-short text a challenge. | Direct Figure 6 and surrounding analysis. Exact point values and uncertainty are not tabulated. The positive result and stated limitation both matter. | partly covered: `vocabulary-diversity`, `sentence-length-variance`, and several density checks have minimum-length gates, while H13 treats length as a register-aware research variable. `dev/TESTING.md` asks for comparable lengths or length-normalised reporting. | Current evaluation does not publish performance strata below 50, 50 to 100, 100 to 200, and above 200 tokens. Current skip rules are engineering thresholds, not validated robustness results. | If approved, add text-length strata to the separate robustness-lane design, report skipped checks and candidate opportunity, and keep the paper's classifier F1 separate from human-eyes metrics. | pending | not started |
| C06: With 839 training samples SSLA is plotted near 0.90, roughly 0.19 above ConDA and 0.13 above LM-D, and remains ahead through 8,397 samples. | Direct Figure 3 and author interpretation. The prose calls the result accuracy, but the figure's vertical axis is labelled Macro-F1 Score, so the metric is internally inconsistent. Exact plotted values and variance are not tabulated. The result concerns supervised training efficiency. | not covered: human-eyes does not train SSLA-like classifiers or report learning curves by labelled sample count. | The metric conflict prevents precise reuse, and the result cannot establish that the current small matched corpus is sufficient for pattern calibration. | Record only. If a future learned comparison engine is approved, require full learning curves, held-out domains, uncertainty, and versioned training data before treating data efficiency as established. | pending | not started |
| C07: Training on Reuters and testing on Essay gives SSLA Macro-F1 0.7083, ahead of OTB-D at 0.6719 and semantic baselines near 0.45, but well below SSLA's in-domain results. | Direct Figure 4 and Section 4.3. It supports relative cross-domain robustness while also showing a large absolute transfer loss. | partly covered: `dev/TESTING.md` requires genre and register breadth, weak and reversed cases, and explicit provenance. The pending Xia et al. recommendation in `pattern-opportunities.md` already proposes a factorial prompt-model-domain robustness lane. | No controlled domain-transfer matrix exists for human-eyes, and ordinary corpus results cannot be presented as cross-domain generalisation. | If approved, add this source as corroboration for the existing pending robustness lane. Report both relative rank and absolute transfer loss, with source and target domains in each direction. Do not alter the ordinary corpus. | pending | not started |
| C08: Under one BBC-news-editor persona prompt applied to GPT-4o, DeepSeek-V3, Gemini-1.5-Flash, and GLM-4 on XSum, SSLA reports Macro-F1 0.952 versus 0.956 for the unconstrained six-model baseline. | Direct Table 2 and Appendix A.7. The adversarial test changes dataset and model count as well as persona condition, and the baseline uncertainty is shown only for the standard result. It is not a same-six-model paired comparison. | partly covered: `dev/TESTING.md` explicitly excludes coached, rerolled, and human-rewrite AI samples from the main new-corpus comparison while allowing separately labelled experiments. `pattern-opportunities.md` already has a pending separate coached or adversarial track. | The project has no preserved persona-steering corpus or protocol. Treating the two reported scores as a clean causal comparison would ignore the changed model set and dataset. | If approved, extend the separate adversarial track with exact prompts, model versions, dataset identity, same-model paired controls, and explicit separation from the ordinary fresh-write corpus. | pending | not started |
| C09: Ablations show full fusion has the best average F1 at 0.956, while the combined static-plus-rewrite Feature variant scores 0.513, rewrite rigidity alone scores 0.441, SN-Grams alone score 0.662, RoBERTa alone scores 0.935, RoBERTa plus SN-Grams scores 0.953, RoBERTa plus Feature scores 0.876, and removing rewrite rigidity scores 0.947. The authors also present an aligned-representation t-SNE plot, a confusion matrix, and a SHAP contribution plot as evidence of discrimination and feature-level interpretability. | Direct Table 5, Figures 5 and 7, and author interpretation. Full fusion and RoBERTa plus SN-Grams improve on the reported RoBERTa-only average, but RoBERTa plus Feature performs worse than RoBERTa alone and most performance comes from the semantic encoder. Full fusion is not best in every domain: its Essay F1 of 0.945 is below RoBERTa plus SN-Grams at 0.952. Figure 5 visualises a representation that combines style and semantic paths but supplies no quantitative cluster analysis. The paper does not report the SHAP protocol, stability, uncertainty, explanation faithfulness, or a user evaluation. | partly covered: `overall-signal-stacking` combines several rule-based weak signals and vocabulary evidence, but it is not trained attention fusion and has no semantic encoder. `STRATEGY.md` keeps pattern accumulation distinct from authorship detection and lists report usefulness as an aspirational metric. | The source does not validate the current aggregate weights, show that adding more surface checks will improve editing usefulness, or establish that its visualisations are faithful or useful explanations. | Record the ablation and interpretability qualifications. Do not cite SSLA as validation of `overall-signal-stacking` or current report explanations. If any research prototype is approved, compare each component, preserve null or weak gains, and test explanation faithfulness and usefulness separately. | pending | not started |
| C10: Changing the rewrite probe from DeepSeek-V3 to Gemini-1.5-Flash leaves reported average F1 close at 0.956 versus 0.954, while individual datasets move by as much as 0.024 accuracy and 0.014 F1. | Direct Table 4. Two probes provide limited robustness evidence, not broad probe independence. Probe versions, prompts beyond the semantic-preserving instruction, run variance, and cost are not fully reported. | not covered: human-eyes has no rewriting probe in its Audit or deterministic grader. | A two-probe average can conceal dataset-specific movement and future model drift. | Record as a limitation on C03. Any approved experiment must use versioned probes, repeat runs, per-domain results, cost and latency reporting, and a no-probe baseline. | pending | not started |
| C11: In the human fan-fiction case, the style path is correct but semantic fusion causes a StableLM error. In the ChatGLM fairy-tale case, the style path labels the text ChatGPT and the semantic path corrects it. | Direct Appendix A.8 case studies. They are illustrative, not prevalence estimates. The human passage has high dependency distance and the ChatGLM passage has a standard opening and high rewrite similarity, showing that plausible style cues can point in the wrong direction. | fully covered as a decision boundary: `STRATEGY.md`, `human-eyes/references/patterns.md`, and `dev/TESTING.md` prohibit treating a surface cue as authorship proof and require context, genre, and weak cases. Focused checks reproduced the C02 current-check results and passed `vocabulary-diversity` and `sentence-length-variance` for both passages. `paragraph-length-uniformity` skipped both because each contained only one substantial paragraph. `overall-signal-stacking` remained below threshold at 2/4 for the human passage and 0/4 for the ChatGLM passage. These checks measure different properties rather than reproducing SSLA. | The exact SSLA path decisions are not reproducible without code or weights, but no product gap follows because human-eyes does not make source labels. | Take no product action. Preserve the cases as cautionary evidence that style metrics can be ambiguous and that learned fusion can both correct and create errors. | pending | not started |
| C12: SSLA is intended for offline forensic analysis because of feature-extraction cost; it depends on high-quality parsers, is limited for low-resource languages, and handles only known closed-set classes rather than unknown generators. | Direct Limitations section. The checklist also reports no dedicated downstream-risk discussion and reliance on the original public dataset curation and licensing. | partly covered: source metadata records language, model, and domain scope; H12 and H25 track genre and model-family dependence; and `STRATEGY.md` avoids authorship claims. The current product does not depend on SSLA's parser or closed-set classifier. | The paper does not establish multilingual, open-world, real-time, fairness, or deployment safety. These absences limit adoption even if attribution research is explored. | Record these limits in this card and require multilingual, unknown-model, latency, licensing, and harm evaluation before any separate attribution prototype. No active product change. | pending | not started |
| C13: The ACL checklist says AI tools assisted grammar, clarity, wording, and submission procedure but not scientific ideas, method, findings, or substantive content; this disclosure is absent from the paper body under the authors' reading of ACL guidance. | Direct author response in the official checklist. It is provenance evidence, not validation of the paper's findings and not a prose-pattern result. | partly covered: source-ingest metadata preserves provenance, and `pattern-opportunities.md` has a pending policy decision on graduated disclosure of AI assistance with human review. No active pattern treats disclosed assistance as a violation. | Human-eyes has no settled vocabulary for distinguishing language editing, coding support, procedural support, and substantive generation. | If approved, use this as one provenance example in the pending graduated-disclosure discussion. Do not create a writing tell or infer undisclosed substantive assistance. | pending | not started |

## Recommendations

- C01: If approved, cite this source under H22 and H25 as closed-set model-family evidence, with matched-control and metadata requirements; no active check change.
- C02: If approved, add the named feature families to a research-only matrix under H22, H23, and H25 and require cross-condition testing before promotion.
- C03: Record rewrite rigidity as research context only unless the user separately approves a versioned, multi-probe experiment outside the product path.
- C04: Record the benchmark scores and product boundary only; do not import classifier metrics or source labels.
- C05: If approved, add text-length strata to the separate robustness-lane design, not to active thresholds.
- C06: Record the data-efficiency result and its accuracy versus Macro-F1 label conflict only unless a future learned comparison engine is approved.
- C07: If approved, add this source to the pending factorial robustness lane and report absolute as well as relative transfer performance.
- C08: If approved, extend the separate coached or adversarial evaluation track with same-model paired controls and exact prompt provenance.
- C09: Preserve the ablation and interpretability qualifications and do not cite SSLA as validation of the current signal-stacking weights or report explanations.
- C10: Preserve probe-specific movement as a limitation and require versioned multi-probe reporting for any future experiment.
- C11: Take no product action; retain the two cases as cautionary evidence about ambiguous style metrics and fusion errors.
- C12: Record the language, parser, cost, closed-set, licensing, and risk-discussion limits; require them as gates only if attribution research is separately approved.
- C13: If approved, add this checklist response as one example in the pending graduated-disclosure discussion without making it a writing tell.

## Evaluation of approved changes

- C01: not applicable - recommendation pending; no product change implemented.
- C02: passed - approved 2026-07-26 via DR-79. DR-79A retuned G9 `sentence-length-variance` from a standard-deviation threshold of 4 to 9.0 in `human-eyes/scripts/grade.py`, and DR-79B added a mean-sentence-length branch to E5 `no-staccato-sequences` at 15.0 words in prose of 300 words or more. Type-token ratio was already B5. Noun ratio, verb ratio, and the two dependency measures were re-queued as register rows POS-03 to POS-06 against Mae's tagger. Verified by the DR-79A and DR-79B assertions in `dev/evals/tests/test_grade.py`; all 22 test files, the generated-pattern check, and register reconciliation pass on 2026-07-26. Calibration in `dev/evals/sentence-variance-calibration-2026-07-26.md`.
- C03: not applicable - rejected 2026-07-26 via DR-79. The rewrite-rigidity vector needs a second model generating a paraphrase at audit time plus BLEURT, BERTScore, and SBERT similarity scoring, so it is not recoverable by reading the text and is neither of the two detector types.
- C04: not applicable - record-only recommendation pending; no product change implemented.
- C05: not applicable - recommendation pending; no product change implemented.
- C06: not applicable - record-only recommendation pending; no product change implemented.
- C07: not applicable - recommendation pending; no product change implemented.
- C08: not applicable - recommendation pending; no product change implemented.
- C09: not applicable - record-only recommendation pending; no product change implemented.
- C10: not applicable - record-only recommendation pending; no product change implemented.
- C11: not applicable - take-no-action recommendation pending; no product change implemented.
- C12: not applicable - record-only recommendation pending; no product change implemented.
- C13: not applicable - recommendation pending; no product change implemented.

## Document review

- **Review status:** passed
- **Review method:** independent source-record reviewer: Codex CLI fresh process, did not perform the extraction
- **Findings resolved:** corrected C09's Feature-ablation label and the overstatement that explicit features consistently help; added the omitted Figure 5 evidence and its limits; clarified that C11 reproduces current-check outputs rather than the paper's feature values
- **Unresolved findings:** none
